# Vesper Design Canon — Session W: Wayfinding Truth-Up + Motion Stance (2026-07-06)

Scope: the final meta-layer session. A cold-implementer simulation proved the CONTENT is navigable but the ENTRY DOCUMENTS lag the campaign — a new coding agent following the Index today would implement the killed picker model and chase four dead file pointers. Plus: record the Motion Stance (the canon currently specifies zero interaction timings). Five items. Rule for this session and forever after: **edit-in-place — corrections go INTO the authoritative table, session logs only cite.**

---

## 1. Canon Index truth-up (the entry point must never contradict a page banner)

- §01 Trip Creation row: replace "blank / place-only / dates-only / place+dates states · home-is-the-form model · light pickers" with the conversation-first claim set (match the page's own banner).
- §02 People & Collaboration row: remove "group room" from its OWNS summary (Chat owns it; People holds Refs).
- §03: fix "Vesper Single Trip Home.html → Trip Creation" → "→ Trip Document".
- §04 gaps: replace the empty "zero open gaps" state with a pointer: "Open items live in Canon Consolidation §06 — that list is authoritative."
- §05 step 3: replace the frozen five-companion checklist with "check the surface's 'consumed by' relationships in §02 — Interaction Surfaces and State System apply to EVERY surface."
- Fix the orphan COMPANION `<tr>` between Trust & Controls and Places; move Discover Detail & Reader System's row out of the §01 CANON table or retag it.
- Sweep every remaining §01 OWNS cell against its page banner; fix any other contradictions found.

## 2. Canon Consolidation edit-in-place repair

- §02 route table: fix the `/trip-place` row IN THE TABLE (redirect stub → ChoosePlaceSheet inline; conversation-first) — delete the stale "sheet · Place picker" text.
- §04 cluster rows: update every ACTIVE pointer to post-rename names — vesper-chat-canon.jsx, trip-document-canon.jsx, trips-home-canon.jsx + trips-home-canon-screens.jsx, itinerary-canon.jsx, onboarding-canon-*, shared-visual-primitives.jsx. Delete the phantom "stay-canon-screens.jsx (pending dedup)" note (stay-system + stay-screens are a sanctioned kit/screens pair).
- Reclassify vesper-chat-kit / vesper-chat-sections / vesper-chat-cards-a / vesper-chat-cards-b as ACTIVE (owner: Vesper Cards.html) and correct §04c's "all archive rows executed" claim.
- §06: delete the amber reasons that §07 session logs already flipped (Chat "old variants remain" etc.) so §06 lists ONLY currently-true open items.
- Add a 20-line "CURRENT STATE — read this first" header at the top: bundle stats (41 pages, ~114 live jsx, all render clean), the canonical file names, the open-items list, and "later sections are history; the tables above are truth."

## 3. Record the Motion Stance board (governance page, next to Reduced Motion + Haptics)

Paste the drafted stance: house curve cubic-bezier(0.2, 0.7, 0.2, 1) everywhere (already live in FocusHome mocks) · sheets present/dismiss 320ms (peek 260ms), scrim in sync · push/pop = native platform default, full-screen edits modal bottom-up 320ms · tab switch = NO transition (only the pill moves; collapse/expand 200ms) · element enter = Cards rule generalized (8px rise + fade, 200ms, stagger 40ms, max 6) · press = scale 0.98 + shadow collapse 120ms global · dismiss = slide + height collapse 200ms, never a destructive flash · toasts enter 200ms/exit 150ms, dwells per IS · skeleton/DotPulse opacity ~1.1s cycle, NO shimmer sweep · riso images fade 200ms · reduced-motion + haptics stances hold · RESERVED bespoke (ship defaults until drawn): ① vote-cast/decision-resolve (+ haptic), ② rail-chip→hero promote + hero posture swap (fhrise 0.32s = placeholder default), ③ deck enter/exit + map face flip ("the transition is the mode change" — deck default: 320ms fade-to-dark; flip default: 200ms crossfade). Rule zero: calm paper — motion settles, never bounces. Update the Cards ICON & MOTION block to cite this board as the global source.

## 4. The two leftover caption rulings + UtilityRow

- Under the Trips Home hero AND Trip Document VoteBand: "Not now suppresses the card for the session; the cascade re-elevates only on material change (new vote, deadline <48h) — never a permanent dismiss."
- Under Trips Home's ON THE TABLE cards: "tap opens the trip's decision — same destination as DraftRow's arrow."
- Add the UtilityRow definition section to Vesper Row System.html (dense sans-led management/reader row — derive from SavedRow's blessed shape: 28px glyph slot, sans 14.5 w500 title, full-bleed hairline) so saved/guide's "blessed 2026-07-06" citations resolve.

## 5. Booking font-link residue + ledger

- Vesper Booking & Reservation.html line ~9: delete the dangling `;500&display=swap" rel="stylesheet"/>` fragment after the canonical font link (dead markup from the unification pass).
- Ledger rows for all of the above; anything skipped = honest OPEN.

## Definition of done

A cold agent following Index → Consolidation → page reaches current truth on every surface with zero dead pointers and zero contradictions · Motion Stance recorded and cited from Cards · captions + UtilityRow in · fragment gone · export.
