# Vesper Design Canon — Session G: Governance Truth + Stances (2026-07-06)

Scope: make the governance layer trustworthy again, record the missing platform stances and seam rules, and clear Session L's residuals. Five items; almost all recording, minimal drawing.

Standing rules: this session is MOSTLY edits to `Vesper Canon Consolidation & Ownership.html` — it is the one page where edits are always in-bounds · no visual redesign · anything skipped gets an honest OPEN row (Session L skipped its item 5 silently — do not repeat that).

---

## 1. Session L residual punch list (finish the language session first)

- trip-document-canon.jsx ~1635/1767/1896: three SWAP attention cards still say secondary "Reject" → "Decline" (per the Verb Canon ruling).
- costs-screens.jsx ~349: prose "No Costs module on Trip Home…" → "…on the Trip Document…".
- Vesper Route & Transport.html ×2: "Full-screen push from Single Trip Home or Itinerary" → "…from the Trip Document or Plan".
- **Execute L's skipped item 5** (copy-family canonization): pick ONE wording each and align — "I'll keep the tab" (7 instances, 4 wordings; owner: Costs; suggest "I'll keep the tab once you start spending."), "Nothing needs you" (6 files, 3 endings; owner: Home calm board; suggest "Nothing needs you — the trip's just unfolding."), ryokan exemplar (5 files, align exactly), Stay's "covered for all five nights" ×2 (align). Then add the 02d row L never wrote.

## 2. One authoritative status view

The governance file itself admits per-row staleness ("trust this summary over the per-row status text") and status now lives in five places (§03 column, §06 amber lists, §06b session banners, §04 EXECUTED banners, 02d statuses). Restructure:
- §03's confidence column becomes the ONLY writable status field.
- Flip the provably-stale ambers to CANON OWNED with a dated note: **Chat, Trips Home, Discover, Onboarding, Auth & Invite** (their recorded reasons — old variants, v2 naming, archive splits — were all executed in the file-surgery session), and **Notifications + Universal Search** from NEEDS ROUTE MAPPING (the 20-row RoutingMatrix and entry-points/route-order boards now exist — verify each once while flipping).
- Collapse the session banners + EXECUTED banners into one dated CHANGELOG appendix, explicitly marked non-authoritative.
- §06's remaining genuinely-open list should shrink to exactly what is still true after this session.

## 3. Platform Stances board (new small board on the governance page)

Record the six absent stances as one-liners; generalize the three partials:
- **Dark mode:** "v1 = light-only; system dark mode not honored; revisit post-dogfood."
- **iPad / landscape:** "v1 = iPhone portrait only; 393×852 frame canon; no landscape or iPad layouts."
- **Android:** "v1 = iOS-only dogfood; no Material affordances or system-back semantics in canon."
- **Haptics:** "Haptics only at explicitly listed moments (long-press per vesper-modes); no haptic grammar in v1."
- **Offline floor:** "Must WORK offline: trip map pack + cached trip/itinerary reads. Everything else = OfflineNotice per State System."
- **Launch screen:** "iOS launch = static brand mark on paper ground; the Onboarding opening remains the only in-flow brand moment."
- Generalize partials: dynamic type ("all surfaces survive XL; Accessibility XXL = reflow-not-clip — the Cards rail rule generalized"), reduced motion ("prefers-reduced-motion disables all nonessential animation — Discover Map rules generalize"), screen reader ("Interaction Surfaces a11y checklist is the floor for every sheet; full VoiceOver pass post-dogfood").

## 4. The three seam rules (one line each, on the owning pages)

- **Cold start → first card (Home; the one real seam gap):** "A brand-new user's home opens on the starter face; the first real deck card arrives only once a genuine signal exists (a trip, a save, an answered question) — the deck is never artificially seeded and never empty-with-apology."
- **Stale notification tap (Notifications routing matrix, one added line):** "Tapping a notification whose subject is already resolved lands on the destination in its resolved state — never an error, never a dead link."
- **Search → entity scroll (Universal Search impl notes):** "Tapping a result opens the destination scrolled to the matched entity where applicable; otherwise top-of-page."

## 5. Small status closures

- **Trips Imminent Hero States:** decide keep-as-companion vs demote to APPENDIX of Trips Home (it is supporting canon with no session work; recommend keep, flip its amber with a dated "reviewed" note).
- **Trip Document "hybrid boundaries" amber:** the Session 7 solo-posture work touched the page; verify the expressive-cover/productive-spine boundary note is current, then close or restate the reason.
- **§02c:** add the `/trip-place` note (route is now a redirect stub in code; ChoosePlaceSheet opened inline from trip-info/settings/cold Folio — conversation-first executed 2026-07-05).

## Definition of done

Zero "Reject"/naming residuals · L's item 5 executed + its 02d row added · §03 is the single status truth with ≤4 genuinely-open ambers and a changelog appendix · Platform Stances board exists (9 lines) · three seam rules recorded on their owning pages · three small closures done · export a fresh handoff.
