# Surface manifest — canon ↔ code correspondence ledger.
#
# One row per product surface. Maps the design canon files that define it to
# the travel-app code that implements it, plus the handoff number it was last
# verified aligned against. `scripts/canon-drift-check.py` reads this file:
# when a new handoff changes a `canon` path, every surface referencing that
# path gets flagged stale — regardless of what `status` says — so staleness
# is never hand-tracked, only hand-cleared after a real re-verify.
#
# Repo root for `code` paths: ~/travel-workspace/travel-app, unless a path is
# prefixed "travel-agent:" — some surfaces (e.g. Universal Search) have real
# backend work too; the prefix disambiguates which repo to open.
# Repo root for `canon` paths: ~/travel-workspace/design/vesper-canon-anchor/project
#
# status values:
#   aligned   — verified against `aligned_at`, no known gaps
#   partial   — verified, but a specific known gap/bug is open (see notes)
#   stale     — verification RESET 2026-07-11 (zero-trust re-audit campaign —
#     see design/reaudit-2026-07-plan.md). The canon↔code MAPPING is still
#     trusted; the VERDICT is not: aligned_at values accumulated across
#     handoffs 108→134 while code moved underneath them (concurrent
#     sessions, batches A-H, CC1-4). `aligned_at` preserved as history
#     ("last verified at"); notes preserved (partial-row residuals are
#     still real leads). A stale row exits ONLY via a fresh audit pass at
#     handoff ≥134 — which must also stamp `code_verified_at`.
#   gap       — a real design/code disagreement exists, needs adjudication
#   deferred  — deliberately not aligning right now (off-wedge / not built)
#   unmapped  — canon surface exists; code correspondence not yet established
#     this session. NOT the same as "gap" — just means nobody's looked yet.
#
# `aligned_at: null` means never formally verified (unmapped/deferred rows).
# Editing rule: when you re-verify a surface, bump `aligned_at` to the
# handoff you checked it against, stamp `code_verified_at` with travel-app's
# HEAD short sha (so code-side movement is trackable, not just canon-side),
# and clear/update `notes` — don't just flip status without recording what
# you checked it against.

surfaces:

  # ── Verified this session (canon frozen at 108; two later handoffs
  #    touched a few of these — see aligned_at per row) ──────────────

  - surface: Stay
    canon: ["Vesper Stay.html", "stay-screens.jsx", "stay-system.jsx"]
    code: ["app/accommodation/[accommodationId].tsx", "components/stay/", "app/trip-accommodations/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 re-audit (2026-07-11) — rare case: the '5/5 confirmed' G1 claim genuinely holds up on direct re-check, including the VESPER PICK backend-gating (still zero 'VESPER PICK' strings anywhere, still honestly undone since no backend field exists) and the blocking-phase/nudge vote-arc, which now has a real backend endpoint (POST /trips/{id}/nudge). Real gaps found beyond G1's scope: StayRow (canon's Trip-Home stay module — icon+label row) is fully built and correctly ported but never wired into the live Trip Home; TripFolioHome.tsx uses a structurally different 'document' row (StayDocumentSection) instead, and that row carries none of canon's vote-pending/hold-expiring attention signal — a group with an open stay-vote sees ordinary copy on Trip Home with no urgency cue, only discoverable by drilling into trip-accommodations. Also: no confirm/release affordance for a held candidate once it's held (backend supports releasing a hold with null, never called). Core lodging-plate/StayCard/NightRibbon primitives and check-in/out data model confirmed exact ports."

  - surface: Costs
    canon: ["Vesper Costs.html", "costs-screens.jsx", "costs-system.jsx"]
    code: ["app/trip-expenses/", "components/expense/", "utils/costsViewModel.ts"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11): the masked:false bug is CONFIRMED FIXED — costsViewModel.ts:393 now threads the real field, with a full fix-arc (fcdb0dbf fix + 30b8f9f6 regression test + df002d62 doc cleanup), spot-verified directly. CostsBalanceSheet's dense-size SheetHeader migration (D21) confirmed correct in this context too. Balance-footer semantics (send-nudges/settle-up/review-balances, never universal 'settle everything'), sheet-vs-drill-in density routing, and settled-state suppression all confirmed exact matches. New gaps found: (1) no trip-day field on Add/Edit expense — hardcoded to `new Date()`, so users can never log a past-day expense even though the ledger is day-grouped; a real functional gap, not cosmetic. (2) ExpenseDetail.tsx has structurally diverged from canon's dashed-card/color-coded-split receipt grammar — it grew real, canon-undocumented features instead (comments thread, 'mark my share as settled', source-proof badge, itinerary link). (3) masked rows show the lock icon but not canon's 'PRIVATE' split-label swap. Also surfaced: the Buttons-family audit's CostsReceiptButton 'notch silhouette' finding may rest on the wrong canon file — Costs' own RBtn spec (costs-system.jsx:171) just says plain pill, matching what's shipped; worth re-checking that finding's source citation."

  - surface: Proposal & Decision Detail
    canon: ["Vesper Proposal & Decision Detail.html", "vesper-proposal-detail-app.jsx", "revert-conflict-sheet.jsx"]
    code: ["components/trip/proposal-detail/", "app/trip-proposal/[proposalId].tsx", "components/receipts/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11) REFUTES the old 'all 5 tasks confirmed' claim on 2 of 4 identifiable items (a 5th was never named, can't verify). ProposalVoteRows and the receipt-dispatch system genuinely hold up — voter avatars, leading/tie logic, and kind-based receipt routing all confirmed exact. But 'RevertConflictSheet 3-states' does not: its 'summary' state is dead code (defined, tested, never invoked from any production call site), and the live 'conflict' state has quietly diverged from canon's per-item Keep/Discard interaction into a backend-resolved binary Force/Keep choice — a real, reasoned redesign (cited K1, 2026-07-06) that canon was never updated to reflect. 'Closed/recovery' also doesn't hold: rejected and plain-withdrawn (non-revert) proposals render NO closed-state banner at all — ProposalClosedState only fires for 'superseded', TrustStatusBanner only for accepted/withdrawn-with-revert. Recovery actions themselves are fine. Also: the Sheets-family audit's `dense` SheetHeader finding is still unresolved here — all 3 RevertConflictSheet title calls still default to `size='large'`. Minor: ProposalDetailScreen hand-rolls its own header chrome instead of reusing the shared ProductiveHeader canon's Chrome wrapper specifies."

  - surface: Changes
    canon: ["Vesper Changes System.html", "changes-kit.jsx", "changes-screen.jsx", "changes-states-board.jsx"]
    code: ["components/trip-plan/ChangeTimelineRow.tsx", "app/(tabs)/trips/[tripId]/changes.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11) REFUTES 'Ask-the-Organizer (net-new)' as a Changes-surface claim — spot-verified directly. BlockedActionRow.tsx has zero references inside changes.tsx or ChangeTimelineRow.tsx (grep confirmed); the pattern actually lives in trip-dates/trip-settings/chat-agency-sheet (permissions surfaces). Its canon source, ask-organizer-pattern.jsx, isn't even loaded by Vesper Changes System.html's own script list (confirmed by reading the HTML directly) — moved off this row's canon: and code: lists to components/trip/ (see People & Collaboration / a future permissions-surface row for its real home). One real spec violation found in the pattern itself: GroupAgencySheet's 'change Vesper autonomy' uses Ask (intensity c) where canon's mapping table says Locked-visible-explained (intensity b, no Ask) — worth a follow-up regardless of which row owns it. What IS genuinely Changes-surface and holds up: row anatomy (SourceChip + serif title + read/unread dot), TODAY/EARLIER sectioning, delegation-as-nudge, votes-in-place with urgent banner, and the masthead headline all confirmed exact matches. Minor: resolved-row 'Undo' renders as a full secondary Button labeled 'Revert', heavier than canon's subtle inline mono-text toggle."

  - surface: Trip Settings & Admin
    canon: ["Vesper Trip Settings & Admin.html", "vesper-trip-settings-app.jsx", "ask-organizer-pattern.jsx"]
    code: ["app/trip-settings/", "app/trip-info/", "app/trip-dates/", "components/trip-settings/", "components/trip/BlockedActionRow.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: >
      Wave 3 re-audit (2026-07-11) REFUTES 2 of the old '4/4 confirmed' claims,
      spot-verified directly. 'Cancel via typedConfirmation' is FALSE — no
      Cancel-trip action exists anywhere in the codebase (only Delete/Archive);
      the sole-organizer leave guard is even missing its canon-specified
      'Cancel trip instead' escape hatch as a direct consequence. 'Delete via
      typedConfirmation' is real but diverges from spec on two axes: canon
      wants the literal word 'delete' typed, code requires the exact trip
      name; canon marks the draft-delete path as untyped, code requires
      typing 'delete' there too.
      SECURITY-RELEVANT, HIGH PRIORITY: the same commit that claimed the
      'role-gating fix' left permissions.tsx's Vesper agency-mode/threshold
      controls (lines 196-223, spot-verified directly) with ZERO isOrganizer
      check — any trip member can silently change the group's Vesper
      autonomy mode and mute the room for everyone, no confirmation, no
      gate. This is the SAME canon rule (ask-organizer-pattern.jsx's
      'change Vesper autonomy' row, spec'd Locked-visible-explained) that
      GroupAgencySheet.tsx independently gets wrong in the OTHER direction
      (uses Ask where canon wants no-Ask) — three implementations of one
      rule, two wrong in different ways. Needs a shared gating primitive,
      not three hand-rolled checks.
      Full Ask-the-Organizer intensity table audited: 4 of 6 rows confirmed
      correctly gated (dates→Ask, remove-member/delete-trip/transfer-organizer
      →locked-invisible); privacy/sharing scope is substrate-gated (no
      editable control exists yet to violate).
      What holds: name-edit dirty-dismiss dialog, member-remove/promote
      gating, most role gates. Minor: destination-row gating is inconsistent
      between trip-settings and trip-info screens for the same action;
      Archive uses the wrong dialog chrome (centered ConfirmDialog vs
      canon's bottom Sheet with itemized reassurance).

  - surface: Trips Home
    canon: ["Vesper Trips Home.html", "trips-home-canon.jsx", "trips-home-canon-screens.jsx", "trips-home-system.jsx", "trips-all-board.jsx"]
    code: ["app/(tabs)/trips/index.tsx", "components/trips/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11): masthead-vs-hero separation, group-posture detection, OnTheTable's ≤2-card/list threshold, /trips/all's Planning+Dreaming sectioning, and urgent dismissal-for-session all CONFIRMED — in a couple places (cascade evaluation order, group-vote-hero escalation) code improves on a literal canon rule with documented in-code reasoning, not drift. Real structural gaps in the trail/section system: TripsHomeTrail collapses canon's 8 independently-labeled sections (Friends/Saved-no-trip/Vesper-noticed/Dream-seeds/etc, each with its own fixed label per canon's Rule 6) into one list under a single rotating shared label ('WHILE YOU'RE HERE' or 'OR START FROM' chosen heuristically) — a Friends row and a Vesper-noticed row can render side-by-side under the same generic eyebrow. Cold-state additionally shows two sections both labeled 'OR START FROM' with Dreams sandwiched between them, breaking canon's specified Hero→Dreams→StartFrom order. TripsFooter renders unconditionally where canon shows it only on some hero states (omitted on Ready/Between/Group). Top-bar shows Search+Bell where the canon file on hand shows Plus+Bell — the code comment claims canon backing but cites a mismatch, plausibly because Universal Search shipped after this canon page was authored (canon may just be stale, not code wrong)."

  - surface: Trip Creation
    canon: ["Vesper Trip Creation.html", "create-kit.jsx", "create-screens.jsx"]
    code: ["app/trip-begin.tsx", "components/trip-creation/TripCreationPrimitives.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11): icon-plate fix re-confirmed pixel-exact. The known serif-vs-sans typography gap is CONFIRMED still open, still deliberately deferred (matches the commit's own note verbatim). New findings on the closing VesperLine claim: the line is present with correct copy/position, but canon's actual pattern for THIS specific line (create-screens.jsx:112) is a distinct muted-italic-no-kicker 'aside' treatment, not the shared VLine/VesperLine primitive (which always carries a 'VESPER' kicker canon specifies but code's shared component never renders, in ANY of its usages app-wide). The fix commit's own description ('italic serif — the right register') is also factually wrong: the shipped style is non-italic serif_semibold. Separately found: the joiner-variant screen (returning users who've only joined trips) never swaps to canon's distinct NewTripJoiner headline ('Your turn to start one.') — it keeps the default headline and adds an extra StateNotice canon's joiner screen doesn't show; this predates and was untouched by the 126 alignment pass. Minor: ShapeCard's selected-check badge is styled inverted from canon (filled-gold vs canon's outline-on-cream) plus a small size delta."

  - surface: Interaction Surfaces
    canon: ["Vesper Interaction Surfaces.html", "vesper-interaction-surfaces-app.jsx", "consequence-banner.jsx"]
    code: ["components/ui/BottomSheet.tsx", "components/ui/ConfirmDialog.tsx", "components/ui/ConsequenceBanner.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 re-audit (2026-07-11): this row's real canon content was NEVER READ before — the HTML is a loader shell pulling in 6 scripts, only 1 of which (buttons.jsx) had ever been checked by any prior audit. Added vesper-interaction-surfaces-app.jsx and consequence-banner.jsx as the real canon sources; sheet-atoms.jsx and field.jsx are also loaded (Handle/Field are thin wrappers over them) but still unread — flag for a follow-up. Against real content: BottomSheet's own cited scrim-weight numbers for peek/chooser/form (0.20/0.28/0.28) are exact matches. But ConfirmDialog's scrim value is a real miss — 0.45 vs canon's 0.40 — and its justifying code comment is factually backwards (claims Center Dialog is canon's 'heaviest' scrim; canon has two heavier: voice overlay 0.50, photo lightbox 0.85). Peek/chooser snap-height caps run ~1.3-1.4x past canon's stated envelope (max-height ceilings, not fixed heights, so short content is unaffected). ConsequenceBanner (added to code: list) implements canon's anatomy correctly but violates canon's explicit 'no circular icon containers' rule with a literal circular icon wrap. Deck.tsx's raw <Modal> gap (previously flagged) is CONFIRMED STILL OPEN, unchanged. BottomSheet's dirty-dismiss/swipe machinery (Batch H2) reconfirmed real and working, though its own doc comment on disableSwipeDismiss is stale (says 'no drag dismiss yet' next to fully-implemented swipe code)."

  - surface: Motion
    canon: ["Vesper Productive Type & Material.html"]  # confirmed near-empty for motion content — see notes; real motion content lives in vesper-canon-consolidation-app.jsx, not yet added as a formal citation pending manifest-owner confirmation
    code: ["constants/motion.ts", "utils/motion.ts"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 re-audit (2026-07-11) confirms the assigned canon file has ZERO motion/animation content (grepped directly: 0 hits for duration/easing/spring/animat) — it's entirely about Stay/Costs type-and-material treatment. The Tokens-family audit (Wave 1) actually cited a DIFFERENT file, vesper-tokens.jsx, for the duration/easing values it confirmed — this row's manifest mapping has likely been wrong since it was created. The real motion-philosophy content (reduce-motion stance, named animation patterns like press/dismiss/toasts) lives in vesper-canon-consolidation-app.jsx §06c, not referenced by ANY row in the manifest. Against that real content: a genuine, systemic gap exists — several components running legacy-Animated-API loops/timings (VoiceOrb, CardStates, CardLive, CardLift, Deck.tsx, OrganizerInviteNudge, ErrorBanner, AtlasMemoryReel, NavChromeContext) have NO reduce-motion check of any kind, contradicting canon's explicit 'disables all nonessential animation, app-wide' rule — while the two sanctioned mechanisms (utils/motion.ts + Reanimated's useReducedMotion) are well-built and correctly used elsewhere. Minor: press-scale drifted 0.98→0.97; press feedback uses a spring where canon specifies a fixed-duration timing collapse. Duration/easing token values themselves remain confirmed-exact per the Tokens audit — not re-derived here."

  - surface: Header system
    canon: ["Vesper Header Audit.html", "header-audit-app.jsx", "productive-header.jsx"]
    code:
      - "components/ui/ScreenHeader.tsx"
      - "components/ui/EditorialMasthead.tsx"
      - "components/ui/CollapsingHeader.tsx"        # CompactHeaderBar
      - "components/chat/FloatingChatHeader.tsx"
      - "app/(tabs)/trips/[tripId]/story.tsx"        # MemoryStoryHeader — the one real fork
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11): 4 of 5 code files confirmed genuinely aligned — ScreenHeader/ProductiveHeader is a near-literal port (minHeight:50, minWidth:52 slots, full prop surface match), FloatingChatHeader's floating-pill Family C is 1:1, and EditorialMasthead/CollapsingHeader are legitimately-ratified Family A registers (D11, doc-only reconciliation, confirmed real). BUT the prior 'aligned' verdict was WRONG on the 5th: story.tsx's MemoryStoryHeader (line ~46) is a hand-rolled fork, NOT a ProductiveHeader wrapper as the old notes claimed — confirmed by direct read, zero ProductiveHeader import in the file, and 'TSPageHeader' (the name canon's ruling cites) doesn't exist anywhere in code. This is exactly the fork design/archive/2026-07/code-alignment-brief-J1-header-consolidation.md correctly identified and explicitly deferred to a future Trip-Story rework (Batch J2) — it was never actually closed, a later session just wrote 'NO CODE CHANGE — all three rulings match shipped code' into this row without re-checking. FIXED the manifest's own self-contradiction (a stale header comment said 'code NOT yet aligned' right above notes claiming the opposite — removed, this row's real status now lives only in status:/notes:). Minor: ScreenHeader's showBack=false fallback branch has zero call sites, dead code. Action: keep MemoryStoryHeader parked pending the Trip Story rework per the J1 brief's own recommendation — do not migrate standalone."

  # ── Notifications/People/Places/Trip Story: touched by handoff 119,
  #    graded via the J1 header recon, not a full surface re-verify ────

  - surface: Notifications
    canon: ["Vesper Notifications and Alerts.html", "notifications-app.jsx"]
    code: ["app/notifications/index.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11) — effectively a first real audit, prior note was header-plumbing only. Filter pills, severity taxonomy/chip colors, mark-read action, and the data-contract fields all confirmed aligned — in places (severity derivation, optional future-fields already modeled) code is ahead of what canon's doc frames as not-yet-available. Two real structural gaps: TODAY/YESTERDAY/EARLIER sectioning is fully implemented (`notificationSectionLabel()`, spot-verified as having zero call sites anywhere outside its own test) but never wired into 3 of 4 canon screen modes — only Trip-Updates mode sections its rows, Global/Trip-Scoped/Personal ship as one flat scroll. The priority/time-sensitive strip has reverted to a bordered, tinted card — exactly the 'NeedsEye card' pattern canon's own doctrine explicitly killed ('no card background', notifications-app.jsx:166-183). Minor: destination-label can diverge from actual tap-navigation target (substrate-gated, canon itself flags this derivation as fragile); kicker isn't always the spec'd muteSoft; loading skeleton doesn't mirror the (mostly avatar-less) row shape; push-denied CTA says 'Turn on' instead of canon's state-dependent 'Open Settings'; stale banner drops the last-synced timestamp and list-dimming."

  - surface: People & Collaboration
    canon: ["Vesper People and Collaboration.html", "people-collab-app.jsx"]
    code: ["app/atlas/companions.tsx", "components/people/", "components/collaboration/", "app/profile/[userId].tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11): the prior brief's PrivateSeam finding ('needs to be BUILT, not adjudicated') is STALE — spot-verified directly. components/chat/group/PrivacySeamDivider.tsx (dated 2026-07-11) is a faithful, unusually well-documented implementation of canon's DignifiedPrivateSeam, explicitly self-citing people-collab-app.jsx:213-220,1041, wired live via DignifiedExceptionCard→GroupThreadItem. Core visual tokens (avatar marker/stack, pill status taxonomy, roster row) confirmed near-exact. But the interaction layer has real gaps: canon's pending-invite MemberActionSheet variant (Resend/Copy link/Revoke) was never wired — pending invites get only an inline revoke icon; the group-chat vote widget is missing canon's per-voter-avatar/tally grammar both in UI AND in its underlying data model (VoteWidgetData has no voter/count fields at all, unlike the itinerary-side PlanDecision which does); self-profile has no 'Edit profile' affordance canon specifies; unfollow has no long-press friction guard canon's caption calls for; the co-organizer role is half-built (present in component props/visual markers, absent from the live TripContext role type, so unreachable in practice — and even the scaffolding is internally inconsistent, RolePill and PeopleAvatar disagree on its color). DecisionInlineRow renders a chevron canon explicitly says the inline itinerary variant shouldn't have. PrivacyMarker is dead code (built, exported, zero consumers)."

  - surface: Places
    canon: ["Vesper Places.html", "places-canon-app.jsx", "hero-chrome.jsx"]
    code: ["app/place/[placeSlug].tsx", "app/venue/[venueId]/index.tsx", "app/trip-place/index.tsx", "components/places/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 re-audit (2026-07-11): the canon-129 header ruling (hero-first, no top bar) RE-CONFIRMED for both live Places screens (place + venue detail); deeper structural rules newly checked (Take placement, inline-not-floating planning rail, neighbourhood Option-A gating) all confirmed exact — strong result for genuinely-unverified territory. MAPPING CORRECTION: app/trip-place/index.tsx is a pure redirect stub (21 lines, no UI) to Trip Creation's ChoosePlaceSheet — canon itself disclaims ownership of this picker ('Trip place picker → Trip Creation, not detail'). It carries no Places content to verify; shouldn't have carried a stale/pending verdict as if it did. Two hygiene items, both previously-known but still live: components/places/PlaceHome.tsx's local VesperMark-that-renders-Fleuron naming collision (Voice-registers finding) is confirmed still present at both its call sites; components/places/SpotActions.tsx is dead code with a self-description that's now false (claims accommodation still depends on it — it doesn't) AND is literally the 'forbidden floating action rail' canon explicitly says not to build — should be deleted, not left importable."

  - surface: Trip Story
    canon: ["Vesper Trip Story and Memory Handoff.html", "trip-story-app-a.jsx", "trip-story-app-b.jsx", "trip-story-app-c.jsx"]
    code:
      - "app/(tabs)/trips/[tripId]/story.tsx"
      - "app/stories/[slug].tsx"                          # public viewer
      - "app/trip-story/share.tsx"                        # Wave 0: orphan sweep
      - "components/memory/ShareStorySheet.tsx"           # OwnerShareControls
      - "components/external-share/PublicUnavailableState.tsx"
      - "components/external-share/ShareLinkStateNotice.tsx"
      - "travel-agent: backend/core/story_projection.py"  # projection builder
      - "travel-agent: backend/core/story_card.py"         # 9:16 card
      - "travel-agent: backend/api/routes/story_landing.py"
      - "travel-agent: backend/api/routes/story_shares.py"
      - "travel-agent: backend/api/routes/plan_similar.py"
      - "travel-agent: backend/core/db/trip_story_shares.py"
      - "travel-agent: backend/core/db/plan_similar.py"
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app + travel-agent HEAD, Wave 2 re-audit 2026-07-11
    status: gap    # most severe Wave-2 verdict — see below
    notes: >
      Wave 2 re-audit (2026-07-11), read against canon fully (not the prior
      07-07 audit's checklist): the lifecycle scaffolding, disclosure-safety
      (unknown/revoked/expired/private all collapse to one generic message,
      matching canon's rule exactly), and provenance/correction UX are
      genuinely high-fidelity — in places exceeding canon (owner-vs-recipient
      routing, "Restore Vesper's version" flow). But the surface's headline
      CONTENT feature is silently broken: visible_places/generalized_place_chips/
      route_cue are hardcoded to []/None throughout the entire backend
      projection builder (story_projection.py — confirmed by direct read, not
      just the agent's citation) despite full schema support and heavy canon
      specification (StoryCardPreview chips, PublicStoryViewer place pills,
      Plan Similar's carried-places seed) — reads as a feature that was
      modeled and scaffolded but the actual "compute chips from story" step
      was never written, no flag/TODO marking it deferred. Canon's "Trip
      group" visibility tier (sign-in-gated viewing by named members) doesn't
      functionally work — falls back to link/public-only behavior server-side.
      MOST NOTABLE: the shipped 9:16 share-card and OG landing page use a
      completely different visual language than canon (indigo accent, system
      sans, no EB Garamond) — and story_landing.py's CSS/placeholder env vars
      are BYTE-IDENTICAL to the unrelated invite_landing.py, strongly
      suggesting the invite surface's generic template was copied wholesale
      without a Vesper brand pass. Owner attribution line ("Shared by X")
      missing from the public viewer. Several ShareLink lifecycle/analytics
      fields (expires_at, replaced_by_share_id, unique_viewers) unimplemented.
      #19 (owner-link routing) IS now fixed — this wave directly confirmed
      "Open in Vesper" owner-vs-recipient routing matches canon exactly
      (app/stories/[slug].tsx:416-450). PRE-EXISTING, NOT RE-VERIFIED THIS
      WAVE (from the 07-07 audit, status unconfirmed on these specific items
      — flag for the next pass): 2 privacy leaks (hidden destination leaking
      via story title; map-thumbnail bypassing RedactionPolicy), 2 remaining
      canon-decision violations (#16 Plan Similar hardcoded, #17 Inspired-Trip
      attribution never rendered), and all 13 canon-named analytics events
      missing (persistence table dropped 2026-06-20). MemoryStoryHeader fork
      (Header-system row) still unmigrated, as previously tracked.

      FIX PASS 2026-07-07 (same day, "fix all identified"): both LEAKs fixed
      — safe_hero_title() now takes hidden_destination and falls back off any
      hidden-destination name-match; story_card.py's map-thumbnail block now
      gated behind `if public.destination is not None`. Attribution copy
      corrected to canon ("Based on a shared Vesper story.", was "Inspired
      by..."). #19 fixed: new GET viewer-context wired end-to-end (schema
      regen + data/http layers + a useEffect in app/stories/[slug].tsx that
      redirects a signed-in owner to Share/Manage). #16 PARTIAL fix only: the
      static "conversation" no longer visually poses as a live Vesper chat
      turn (title "Talk it through with Vesper" → "Your starting point",
      fake speaker label → "WHAT WE'LL START WITH" caption) — this is an
      honesty fix, not the real fix; a true conversational entry needs new
      pre-auth stash-and-resume infra mirroring utils/invitePending.ts and is
      explicitly deferred as separate scoped work. #17 fixed: trip_summary
      .public_story_source.source_attribution now renders as a quiet caption
      in TripFolioHome's cold-start panel (mirrors the destination-read
      pattern), cleared once the trip leaves cold mode. Deliberately NOT
      built in this pass (flagged as follow-up, not silently done): 8-field
      RedactionPolicy expansion (photos/places/route toggles), ShareLink
      lifecycle fields (paused_at/rotated_at/expires_at/unique_viewers),
      a distinct full pre-publish preview screen, and the analytics
      pipe rebuild (13 events still write to nothing — table dropped
      2026-06-20). Tests: 35/35 backend (test_story_projection.py +
      test_story_card.py), 7/7 + 2/2 new frontend (public-story.smoke +
      TripFolioHome.storySourceNote), full tsc --noEmit clean both passes.

  # ── Known canon surfaces with no code-mapping work done this session.
  #    Listed explicitly so the manifest doesn't silently look complete. ──

  - surface: Itinerary
    canon: ["Vesper Itinerary.html", "itinerary-canon.jsx", "row-system.jsx"]
    code: ["app/(tabs)/trips/[tripId]/plan.tsx", "components/trip-plan/PlanBlockRow.tsx", "components/trip-plan/ChangeStudioSheet.tsx", "components/trip-plan/ReviewStackSheet.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11), scoped to the screen/sheet layer only — PlanBlockRow's row-grammar was already fully covered by the Row System family audit, not re-derived here. The long-tracked date-rail conflict-dot gap is CONFIRMED STILL OPEN: conflictsByDayId is in scope in plan.tsx and used elsewhere (review-badge, conflict count) but never threaded into the date-rail's own dot computation, so a conflicted day and a day with a benign recent edit render the identical generic dot — no rail-level shortcut exists to find which day has a conflict. NEW: ReviewStackSheet.tsx uses colors.purple for its 'GROUP DECISION' kicker and 'why flagged' rule — a real violation of BOTH the itinerary canon's explicit 4-tone-only rule ('No purple, pink, or novelty accents') AND the app-wide color doctrine that reserves violet for sanctioned Discover/agent/privacy contexts only; canon's own reference board uses blue for this exact kicker. Two lower-severity, self-documented-as-deliberate gaps: ChangeStudioSheet's flat action list vs. canon's sectioned PLAN/MARK/VIEW/REMOVE hierarchy (explicitly flagged in-file as Phase-0 scope, Phase-2 pending), and no held-booking warning banner (capability exists, canon's explanatory framing doesn't). Screen chrome (PlanFocusedHeader, map/list face-toggle, no-bottom-bar doctrine) all confirmed aligned, including well-documented deliberate departures from canon's literal prose."

  - surface: Chat
    canon: ["Vesper Chat.html", "chat-narration-band.jsx", "vesper-composer.jsx", "vesper-chat-canon.jsx"]
    code: ["app/(tabs)/concierge/chat.tsx", "app/(tabs)/concierge/history.tsx", "app/conversations/create.tsx", "components/chat/", "components/voice/NarrationLeaseConflict.tsx", "utils/narrationBandStack.ts"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial    # trending toward aligned — fastest-improving row this wave
    notes: "Wave 2 re-audit (2026-07-11): D17/D20 both re-confirmed correctly shipped. Spot-checked the 07-10 ledger (design/chat-surface-audit-2026-07-10.md) and found it partially self-stale — its own Resolution Log (top of doc) already knew 4 batches were fixed 07-10, but the detailed per-finding sections further down still presented those same items as open with no cross-reference; a reader following the brief's instruction to check the detailed findings would be misled into re-flagging fixed issues. 6 of the ledger's ~20 findings are now fixed, 2 dated literally the same day as this audit (privacy-seam divider now built and wired — components/chat/group/PrivacySeamDivider.tsx, matches People & Collaboration row's finding; TripShapes' emoji/purple-action drift fixed). New, previously-unflagged: history.tsx's ThreadHistoryRow shows the thread's own title-initial where canon's ThreadRow always shows a fixed serif 'V' Vesper monogram — every history row should visually read as 'a conversation with Vesper', this doesn't. Minor: composer literal '...' vs canon's true ellipsis, one more hyphen-vs-middle-dot site. Still genuinely open (re-confirmed, not new): ChatActionRow secondary-action register, GroupEventLine italic misuse on system/join lines, GroupVesperNote's compact-variant register swap, offline cached-thread signal, PrivacyHandoffCard still orphaned outside the dev gallery. Voice-surface audit (2026-07-11): added components/voice/NarrationLeaseConflict.tsx and utils/narrationBandStack.ts — the narration-arrival-banner system (GeofenceBanner/LiveCompanionBanner/NarrationLeaseConflict, stacked+prioritized by narrationBandStack.ts) is genuinely this row's territory (chat-narration-band.jsx already listed here) and CONFIRMED-ALIGNED, in one place improving on canon with a documented reasoned deviation (a geofence hit for the same companion session's next stop gets its own row instead of collapsing, since canon's collapse rule targeted only unrelated sessions). NarrationLeaseConflict.tsx physically lives under components/voice/ but is conceptually Chat's narration system, not live-mic — see the Voice row for that split."

  - surface: Chat — Planning-Progress Cards (TripShapes / ResearchCard / PlanReadyCard)
    canon: ["proposals/planning-progress-cards-canon-proposal.md", "planning-progress-cards-canon.jsx"]
    code: ["components/chat/TripShapes.tsx", "components/chat/ResearchCard.tsx", "components/chat/PlanReadyCard.tsx"]
    aligned_at: null
    status: pending-canon
    notes: "Shipped (31d58f46) with no canon specimen — flagged in chat-surface-audit-2026-07-10.md. 2026-07-10/11: two live violations fixed in code (TripShapes' colors.agent.action purple CTA -> colors.vesper.goldDeep; literal emoji shape badge -> typographic monogram) and visually verified live (rgb(138,102,40) = exact goldDeep hex, browser-confirmed). planning-progress-cards-canon.jsx is a code-first interactive board (phone-shell + CH tokens, matching vesper-chat-canon.jsx's format) covering TripShapes (unselected/selected+CTA), ResearchCard (4 step statuses — confirmed ink/paper palette, not a stoplight system), and PlanReadyCard (whole/partial-progress-line/revision-editor states). Still status: pending-canon — a designer needs to formally ratify/adjust, this session authored it but isn't the design owner."

  - surface: Route & Transport
    canon: ["Vesper Route & Transport.html"]
    code: ["components/trip-map/", "hooks/useOptimizeDayRoute.ts", "data/transportGaps.ts"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 — first real audit (2026-07-11). Removed hooks/useRouteTrip.ts and app/your-map.tsx from code: — both confirmed out of scope (useRouteTrip is an unrelated expo-router trip-resolution helper, naming coincidence; your-map.tsx is Atlas's cross-trip memory map, a different surface entirely, own doc header confirms). This canon file is UNUSUALLY implementation-grade — 1,122 lines, not a loader shell, reads like a spec written from the shipped code — and the match is correspondingly strong: map-as-second-face routing architecture, suppress rules, bottom-sheet snap heights, offline-chip states, and stop-row shape all confirmed exact, including one case where code comments literally cite this canon file by name to justify a routing decision. Real gaps: the bespoke parchment Mapbox tile style is honestly self-documented as not-yet-shipped (substrate-gated on an external Mapbox Studio asset, plumbing already built to flip it); neighborhood card is missing canon's 'n saved here' affordance (no backend field exists for it either); route-line color/dash is keyed on TRAVEL MODE in code vs canon's BOOKING-STATUS taxonomy (confirmed/missing/gap transit lines) — a legible promise ('this leg isn't booked') that isn't currently visible on the map itself. canon-owes-code: a real, live, backend-integrated 'Optimize route' TSP-reorder feature has zero canon documentation."

  - surface: Universal Search
    canon: ["Vesper Universal Search.html", "universal-search-app.jsx"]
    code:
      - "components/search/UniversalSearchOverlay.tsx"
      - "app/search.tsx"
      - "hooks/useUniversalSearch.ts"
      - "utils/universalSearchRouting.ts"
      - "utils/universalSearchTelemetry.ts"
      - "utils/api/universalSearch.ts"
      - "travel-agent: backend/search/dispatch.py"
      - "travel-agent: backend/core/db/search.py"
      - "travel-agent: backend/core/vector/atlas.py"
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app + travel-agent HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: >
      Wave 3 re-audit (2026-07-11): the amber-vs-violet adjudication and
      3 of 5 deferred items (Atlas 3-way convergence, Phase-4 semantic
      conversations, backend interpretation-field) spot-checked and still
      accurately described. TWO deferred items were SILENTLY BUILT since
      the prior verification and had never been checked against canon:
      Receipts/Costs/Actions groups, and the Nearby chip. Checking them
      surfaced real, previously-unreviewed drift. The Actions group ships
      as a read-only, non-tappable audit log (route always null, disabled
      in the UI) where canon specifies executable command rows ("must be
      executable... never decorative") — a self-documented, deliberate
      architectural pivot, but never reconciled with canon, needs a
      product call. Places results never distinguish the user's own saved
      places from public Discover venues — provenance is hardcoded to
      "DISCOVER"/public always, the personal-saves table is never
      consulted, contradicting canon's explicit saved_by contract. The
      on-trip scope-chip rail grew to 7 chips (one per newly-shipped
      group) against canon's explicit "max 4-5" rule — a direct side
      effect of shipping Receipts/Costs/Actions without folding them back
      under "This trip." Empty-state's "Recently Opened"/"In Progress"
      sections are missing — substrate-gated, no recently-viewed-entity
      tracking infra exists yet. Core architecture (10-group taxonomy,
      provenance-required doctrine, fixed group ordering) still confirmed
      solid.

  - surface: Booking & Reservation
    canon: ["Vesper Booking & Reservation.html", "booking-app.jsx", "booking-kit.jsx"]
    code: ["components/booking/", "app/booking/[sessionId].tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 — first real audit (2026-07-11). Removed app/experience/[experienceId].tsx from code: — confirmed disconnected from the booking component family (its 'Book' CTA is a bare external-URL open, no session/receipt tracking, own header comment cites Discover as its real canon); re-homed to the Discover row. The core trust architecture is faithfully ported, in places verbatim: category vocabulary (6 types), state/color language, and the 'hold ≠ booked, never overclaim confirmed' discipline all match canon closely; live-checkout's dark-flag gating (EXPO_PUBLIC_LIVE_BOOKING_ENABLED) is a 1:1 match to canon's own stated v1-dark framing. Real gaps, read as intentional first-slice narrowing rather than drift: no manual/phone-only handoff branch in the session screen (canon specs one); hold states show a static deadline, not canon's live mm:ss countdown; confirmed-booking receipt drops 'View in plan'/'Contact provider' actions down to Cancel-only; group/shared-booking states (organizer-books-for-group, waiting-on-approval) entirely absent, no flag found gating them. canon-owes-code: BookingOfferRow's trade-off pills are a real feature ahead of canon's single static reason-string spec — worth folding back if considered final."

  - surface: Trust & Controls
    canon: ["Vesper Trust & Controls.html", "trust-kit.jsx"]
    code: ["components/trust/TrustControlsKit.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial    # Atlas-coupled, but real live bugs found — see below
    notes: "Wave 3 — first real audit (2026-07-11), ATLAS-ADJACENT as previously noted (all 11 call sites are app/atlas/*), but current-state findings hold regardless of the freeze decision — these are live bugs in shipped screens today. Real: canon explicitly flags a 'post-deletion goodbye' terminal screen as one of two remaining gaps to close — still missing; account deletion drops the user cold onto generic sign-in with zero acknowledgment. Default toggle-ON fill is a diluted gray (trust.mute/ink60) not canon's near-black ink — affects the app's 4 most common toggle rows (notification prefs). One state (PHONE NEEDED) bypasses the shared TrustStatePill component entirely, rendering as a chrome-less raw label inconsistent with its sibling states in the same row. Confirmed dead code (4 exported primitives, verified via direct repo-wide grep, zero call sites) — one of them, TrustGroup, has also structurally drifted from canon's Zone-alias behavior. Core geometry/tokens/typography (toggle size, row heights, Zone/Stamp structure, roman-by-default Contract typography) all confirmed precise. NOT verified this pass: trust-screens.jsx and account-lifecycle.jsx (2 of 4 sibling canon files, define the actual screen bodies) were out of scope — a genuine follow-up needed for full screen-composition fidelity."

  - surface: Onboarding
    canon: ["Vesper Onboarding.html", "onboarding-canon-invite.jsx"]
    code: ["app/onboarding.tsx", "app/onboarding-safety.tsx", "components/onboarding/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 re-audit (2026-07-11), scoped to everything D22/InvGift didn't already cover (both re-confirmed still correct, not re-derived here). The core sequence, state machine, and cross-surface handoff into Auth (via onboardingIntent/authContext) are well-built and faithful — real working engineering. Two genuine new gaps: (1) the trip-in-mind lane now routes through two taste-collection screens (interest + pace) before reaching sign-up, directly contradicting canon's stated principle that a trip-in-mind user 'goes straight to the trip' with 'no decorative quiz that changes nothing' — the captured signals ARE wired into real downstream consumers, so this reads as an undocumented, coherent product decision needing adjudication, not dead scaffolding. (2) the D22 Fleuron→VesperMark fix's own bug class recurs in 5 more onboarding spots the original fix didn't reach (Cover's brand mark, Gift/Permission button icons, TripCaptureFrame's talk-through line, ContextualAuthShell's sign-in kicker). Also: taste-collection UI ships as a conventional row-list where canon specifies full-bleed gouache tiles; app/onboarding-safety.tsx (dietary/allergy capture) has no canon counterpart at all."

  - surface: Auth and Invite
    canon: ["Vesper Auth and Invite.html", "auth-invite-app.jsx", "onboarding-canon-invite.jsx"]
    code:
      - "app/invite/[slug].tsx"
      - "app/invite-code.tsx"
      - "app/(auth)/"                                     # Wave 0: orphan sweep
      - "components/auth/"                                # Wave 0: orphan sweep
      - "utils/inviteToken.ts"
      - "utils/invitePending.ts"
      - "utils/inviteErrorTaxonomy.ts"
      - "hooks/useCreateInvite.ts"
      - "hooks/useTripInvites.ts"
      - "data/invites.ts"
      - "components/onboarding/DeferredDiaryGiftSheet.tsx"
      - "components/onboarding/AuthProviderStack.tsx"      # Wave 2: load-bearing for SignInImpl/SignUpImpl
      - "components/onboarding/ContextualAuthShell.tsx"     # Wave 2: load-bearing
      - "components/onboarding/OtpCodeInput.tsx"            # Wave 2: load-bearing
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: >
      Wave 2 re-audit (2026-07-11) FINALLY checked the sign-in/sign-up/OTP
      code this row's own prior note flagged as never verified — this is
      genuinely one of the best-built surfaces in the app, but the first
      real look found real gaps. OTP wrong-code/expired inline visual states
      (red digit boxes, inline banner) are fully built into OtpCodeInput.tsx
      but neither SignInImpl nor SignUpImpl ever passes the error prop —
      errors surface via toast only, canon's copy ("Code expired.") is lost.
      Safety-chip vocabulary has drifted from canon's specific 6+5 list
      (constants/intake-chips.ts ships a different 8+3 set, missing 3
      accessibility chips canon names, adding one it doesn't). Canon's
      dedicated "Picking up where you left off" pending-handoff restoration
      screen was never built — the OTP screen's own loading spinner is the
      only continuity signal during post-auth resolution. Pre-auth signal
      chips shifted from canon's multi-select taste-tag model to single-choice
      (plausibly a deliberate backend redesign — question_prompt/chip_answer
      are both singular — canon just wasn't updated). GOOD NEWS: the
      reason-disclosure privacy model (collapsing revoked/expired/trip-gone to
      identical copy for untrusted pre-auth viewers) is real, well-reasoned,
      and undocumented in canon — a genuine uncredited hardening, not a gap.
      The apparent "8th invite-error code" mismatch resolved cleanly on
      investigation — it belongs to a different (organizer-side) flow, not a
      taxonomy gap. Core OTP state machine, resend cooldown, OAuth tail, and
      value-before-auth ordering all confirmed solid.
      KNOWN GAP (see project_batch_efgh_alignment / project_vision_alignment_assessment):
      the outbound half of the invite loop (joiner→organizer conversion) was
      completely absent as of 07-06 — FIXED same day via
      ACTIVATION_LOOP_CLOSED telemetry + Batch I, not part of this canon
      page's original scope but tightly coupled to it.

  - surface: Photo & Media Intake
    canon: ["Vesper Photo & Media Intake.html", "vesper-photo-intake-app.jsx"]
    code: ["components/photo-intake/", "app/trip-photos/find.tsx", "components/trip/FindPhotosSheet.tsx", "components/trip/PhotoLightbox.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial
    notes: "Wave 3 — first real audit (2026-07-11); the .html is a loader shell, added vesper-photo-intake-app.jsx (1250 lines) as the real content. State-machine level (permission states, learning-consent separation, story-scoped picker) is solidly aligned. But several product-meaningful capabilities canon specifies don't reach users: the signature day/time/place candidate clustering doesn't exist in the main find-photos flow (flat 3-col grid instead); the destructive 'Remove from trip' deletion canon specs is UI-present but never wired (the component's own onRemove prop is silently only ever used for 'make private' — no delete API exists anywhere in the codebase, so real removal is currently impossible, not just unwired); 'Ask Vesper about this photo' and 'Hide from my view' actions are speced but dead/absent; retag is owner-gated where canon grants it to any member on read-only photos; the AI-suggested story-slot state ('suggested') is unreachable end-to-end (data type doesn't carry it). Default visibility is unconditionally 'group' even solo, where canon's stated doctrine says private-by-default (canon itself is internally split on this one — its own mockup shows a conditional group-default variant)."

  - surface: External Sharing & Public Entry Points
    canon: ["Vesper External Sharing & Public Entry Points.html", "vesper-external-sharing-app.jsx"]
    code: ["components/external-share/", "app/public/place/", "app/public/venue/"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 — first real audit of this surface (2026-07-11). The privacy-critical spine is implemented with unusual precision, in places exceeding canon: reason-disclosure taxonomy matches word-for-word (including the exact generic-collapse copy), owner-CTA suppression, brand-light no-bottom-nav chrome, and 'not found never confirms/denies existence' are all confirmed exact. Two files here (PublicUnavailableState.tsx, ShareLinkStateNotice.tsx) are genuinely shared with Trip Story's code: list, not a mapping error. Real gaps: PrivateSurfaceGate — canon's dedicated answer to protecting costs/decisions/itinerary from link-walkers — is fully built to spec but wired into ZERO app screens (orphaned, likely an anticipated follow-up PR that never landed); the 'Ask Vesper' secondary CTA canon specifies for place entries is absent from both public place/venue routes even though the underlying PublicOpenInAppCTA component supports it and the pattern is used elsewhere in the app; only 1 of canon's 3 place-entry recipient modes (plain public) is implemented — no person-attribution mode, no trip-scoped-hidden mode, even though the route already threads an optional tripId the receiving screen ignores. Minor: privacy-note copy runs 2 sentences against canon's explicit one-sentence rule; no dedicated tests exist for the two public route files despite being unauthenticated and privacy-sensitive. One doc-catchup item: ExternalShareShell deliberately dropped canon's linkState/disclosureLevel props in favor of routing those through PublicUnavailableState/ShareLinkStateNotice directly — a documented, sensible redesign; canon's component table should be updated to match, not the other way around."

  - surface: Guide & Collection Reader
    canon: ["Vesper Guide & Collection Reader.html", "vesper-guide-reader-app.jsx"]
    code: ["components/guides/", "app/guide/[slug].tsx", "components/discover/reader/ReaderChrome.tsx", "components/discover/reader/ReaderFooterActions.tsx", "components/discover/reader/ReaderMemberRow.tsx", "app/dossier/[dossierId].tsx", "app/angle/[angleId].tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial    # Discover-coupled, per prior note — current-state findings still real
    notes: "Wave 3 — first real audit (2026-07-11), DISCOVER-ADJACENT as previously noted. The shared-reader thesis holds up well: ReaderChrome/FooterActions/MemberRow/GuideHeader are genuinely reused identically across guide/dossier/angle, ownership-boundary file layout matches canon's explicit decisions, and MemberRow's dense visual grammar (thumbnail rules, angle-always-no-thumbnail, sequence-vs-glyph leading slot) is a faithful port. Real gap: canon's headline 'Ask Vesper bar' (§06 — quiet/strong states, contextual placeholder copy) was replaced by a materially different pattern — a row of separate always-strong-styled pill buttons — with a 'more' (3-dot) menu from canon's Chrome entirely unported. BUT: ReaderFooterActions/ReaderMemberRow's own code comments cite a DIFFERENT canon file ('Vesper Discover Detail & Reader System.html') as their source of truth, not this row's assigned file — plausibly this was intentionally redesigned against that newer canon, unread by this audit. Needs a follow-up reading that file before treating the AskBar finding as a regression vs. an intentional supersession. Several illustrated states (guide-level save icon, Vesper voice line, in-plan tick) are substrate-gated — the code slots are built and ready, backend fields don't exist yet, self-documented in-code."

  - surface: Saved & Collections System
    canon: ["Vesper Saved & Collections System.html", "vesper-saved-collections-app.jsx"]
    code: ["components/saved/SavedObjectRow.tsx", "app/atlas/saved-places.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 3 re-audit 2026-07-11
    status: partial    # Atlas-coupled per prior note — current-state findings still real
    notes: "Wave 3 — first real audit (2026-07-11), ATLAS-ADJACENT as previously noted, but confirmed genuinely live (8 real entry points across atlas/discover/trips, not dead scaffolding). SavedObjectRow correctly composes the shared EditorialListRow/RowCore primitive (canon names a differently-named 'UtilityRow' variant that doesn't exist in code — doc drift, not a defect, since the delivered anatomy matches). Real, structural gap: 3 of canon's 5 core row capabilities are unreachable from the shipped data path, not just unstyled — the trip-relevance state grammar (In plan/Stale/Unavailable) is fully built in the row component but the screen's data-mapping function never sets the `state` field at all; save-date/recency is likewise dead on both ends (type has the field, mapper hardcodes null); and `provenance` — meant to hold 'Saved from Guide: X' attribution — is populated with generic venue marketing copy (curator_hook) instead, a live content-integrity risk since the field's name promises something it doesn't contain. This reads as a two-phase build where the row/view-model layer shipped ahead of the backend work meant to feed it. Bulk-manage mode and multi-entity-type support are correctly self-documented deferrals (handoff doc explicitly de-scopes both), not undiscovered drift."

  - surface: Global Chrome
    canon: ["Vesper Global Chrome.html", "vesper-global-chrome-app.jsx", "tab-bar.jsx"]
    code: ["components/nav/FloatingTabBar.tsx", "app/(tabs)/_layout.tsx", "app/_layout.tsx", "app/index.tsx", "components/PushAskHost.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, Wave 2 re-audit 2026-07-11
    status: partial
    notes: "Wave 2 re-audit (2026-07-11) did the token check the prior note flagged as never done: radius (30) confirmed exact; border confirmed aligned; blur is deliberately absent app-wide (expo-blur isn't in the build — consistently rgba-substituted in CollapsingHeader/GlassSurface too, substrate-gated not an oversight). But background alpha is a REAL miss — code's colors.tint.parchmentGlass is 0.78 opacity vs canon's specified 0.92 (a visibly more see-through pill), and that token's own inline comment says it was authored for a different surface entirely ('trip card postcard'), borrowed here rather than the nav bar owning its own reconciled token. Also found: collapsed-circle size is 56 vs canon's 52 (previously unflagged). Badge policy re-confirmed NO badge (correct), but the badge-count computation + styles are dead code that actively contradicts its own file's doc comment claiming '4 bottom tabs with badge dots' — a badge feature was clearly built then pulled per the D2 ruling without cleanup. STRUCTURAL FINDING: this canon file set has ZERO content about app-shell/boot/root-layout concerns — every line of app/_layout.tsx, app/index.tsx, PushAskHost.tsx (Clerk gating, splash timeout, push-permission-ask flow) is unfalsifiable against Global Chrome specifically; the Wave-0 'closest fit' mapping was weak. ErrorBoundary.tsx re-homed to State System (family) this wave — its non-dev fallback explicitly defers to ErrorState, which is that row's real territory. app/_layout.tsx/app/index.tsx/PushAskHost.tsx have no better canon home found yet; left here as the least-wrong fit, flagged as effectively unmapped-in-spirit."

  - surface: Discover (+ Detail/Reader, Map & Spatial Browse)
    canon: ["Vesper Discover.html", "Vesper Discover Detail & Reader System.html", "Vesper Discover Map & Spatial Browse.html", "discover-cover.jsx", "discover-cover-polished.jsx", "discover-cover-polished-v2.jsx", "Vesper Discover Cover - Polish.html", "Vesper Discover Cover - Polish v2.html"]
    code: ["components/discover/", "app/experience/[experienceId].tsx"]    # Wave 3: re-homed from Booking & Reservation — its own header comment cites Discover Detail & Reader as its canon, own route ownership disclaims Booking
    aligned_at: null
    status: deferred
    notes: "Off-wedge per project_vision_alignment_assessment — part of the ~35-45% off-wedge surface the Atlas-freeze decision covers. Do not spend alignment effort here until that decision is made. Canon 126 added a Discover Cover polish v2 (discover-cover.jsx +261 lines, 2 new polished variants + 2 pages) — a whole design workstream landed here; still DEFERRED, do not build."

  - surface: Atlas (all)
    canon: []  # no .html pages in the mirror — Atlas runs its own separate canon campaign, see project_atlas_canon_state
    code: ["app/atlas/", "components/atlas/", "components/me/", "app/your-map.tsx"]
    aligned_at: null
    status: deferred
    notes: "~34 screens / ~22k LOC, own canon campaign outside this mirror. Atlas-freeze decision from project_vision_alignment_assessment is STILL OPEN — re-raise before spending alignment effort on this cluster. Wave 0: added components/me/ (TravelDNACard — taste-profile card, see project_taste_store_reconciliation memory) found by the orphan sweep."

  - surface: Voice (live mic / voice chat)
    canon: ["vesper-home-voice.jsx", "vesper-modes.jsx"]    # audio voice ≠ "Voice registers (family)" (typographic/attribution voice, VesperMark etc.) — narration-arrival banners re-homed to the Chat row (chat-narration-band.jsx)
    code: ["components/voice/VoiceOrb.tsx", "components/voice/VoiceOverlay.tsx", "components/voice/VoiceOverlayProvider.tsx", "components/voice/MicPrivacyDisclosure.tsx", "components/voice/MicStatusIndicator.tsx", "context/MicSessionContext.tsx", "hooks/useVoiceSession.ts", "hooks/useNarrationWithInterruption.ts", "components/narration/NarrationListenButton.tsx"]
    aligned_at: 134
    code_verified_at: 543e2bbf    # travel-app HEAD, 2026-07-11
    status: gap
    notes: >
      2026-07-11 audit CORRECTS Wave 0's "no canon page found" —
      that was wrong, a grep sweep with narrower terms missed it.
      Real, substantial, non-archived canon exists:
      vesper-home-voice.jsx (234 lines) specs a full-screen dark
      "Calm Listening" takeover (tap mic → breathing gold ring, live
      waveform, serif transcript, promotes into the thread as text
      with "SPOKEN · timestamp" tags on continued exchange) plus a
      separate hold-to-talk "walk-and-talk" Editorial Transcript
      mode; vesper-modes.jsx specs exactly 3 entry points (composer
      mic tap / long-press the Vesper tab icon / tap the proactive
      dot) with explicit anti-goals ("no floating mic button, no
      wake-word, no Siri-style takeover"). Shipped code diverges
      structurally: a bottom-sheet LiveKit modal, not a full-screen
      takeover; no hold-to-talk gesture anywhere; purple accent
      color (VoiceOrb/VoiceOverlay) where canon is consistently
      amber/gold AND where the app's own color-system doc says
      purple is legacy/agent-presence-only, out of scope for voice
      either way.
      Reachability is mostly dark, self-documented: VOICE_ENABLED
      defaults false, gated consistently at every entry point
      (verified, no bypasses). Even with the flag on, useVoiceSession's
      _connectToRoom is a documented no-op stub — no real audio
      connects. MicStatusIndicator is fully built, tested, and
      documented but never mounted anywhere (dead code).
      useNarrationWithInterruption is a fully-built, ~1000-line-tested
      hook with zero production callers.
      Real bug, independent of the canon gap: MicPrivacyDisclosure's
      shown copy ("mic stays on during narration... VAD runs
      on-device... you'll always see a clear indicator") describes
      the never-wired interruption flow and the never-mounted status
      indicator — users see a privacy disclosure for a feature that
      cannot currently happen, before opening the (also-incomplete)
      live voice chat.
      This is real, substantial design work sitting unbuilt (not "off
      wedge") — a Claude Design ask is NOT what's needed here; a
      build decision is: is vesper-home-voice.jsx's full-screen model
      still the target before investing further in the current
      bottom-sheet shell?

  # ── Shared-element FAMILIES (cross-cutting primitives, not screens) ──────
  #    Added 2026-07-09 from the component-consistency audit. These rows exist
  #    so canon-drift-check.py flags the shared primitives stale when a handoff
  #    touches their canon files — the gap that made every prior component audit
  #    re-derive these from scratch. Evidence + full findings:
  #    design/component-consistency-audit-2026-07-09.md. Open DESIGN-GAPs (D#)
  #    are adjudicated in adjudication-brief-2026-07-09-component-consistency.md;
  #    code work in code-alignment-brief-CC1..CC4.

  - surface: Buttons (family)
    canon: ["buttons.jsx", "Vesper Button Decisions.html"]
    code: ["components/ui/Button.tsx", "components/ui/InlineChip.tsx", "components/stay/StayCard.tsx", "components/expense/CostsPrimitives.tsx"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11) CONFIRMS the canon-126-specific claims (destructive/warning/labelColor/InlineChip/StayButton fold) genuinely landed and still match — that part of the old note was accurate. But the blanket 'fully aligned to canon 126' framing was overstated; real gaps outside that narrow scope survived unaudited: (1) `ghost` variant is missing canon's specified hairline border entirely (buttons.jsx:38 vs Button.tsx:252-265) — affects 7 live sites (ConfirmDialog, PermissionRationale, ExpenseDetail, MicPrivacyDisclosure, ProposalDetailScreen x2, PlanEmptyDay). (2) canon's `amber` variant has no Button.tsx home — mitigated by StayButton's tone-parameterized wrapper reaching the same gold/amber tones, but the formal API gap is real. (3) code ships `secondary`/`success`/`tint` variants canon's taxonomy doesn't name (success plausibly = canon's evolved sage post-CC4.3, undocumented). (4) CostsReceiptButton's entire stated reason for staying separate — a 'ticket silhouette with punched notches' (D14) — was never built; CostsPrimitives.tsx renders a plain full pill, zero notch styling anywhere in the file. This directly contradicts the OLD note below claiming 'CostsReceiptButton left separate (canon-BLESSED, ticket silhouette)' — the silhouette itself doesn't exist. Prior note retained for history:
      CC1.1d (07-09, commits 9038fef2..4272b385) shipped canon 126's full taxonomy — 'adopt canon for all' directive. warning/labelColor/InlineChip/destructive-rename/StayButton-fold all verified. InlineChip ADOPTED in PlanBlockRow. Radius ratchet 374→370. tsc 0, ratchets green, tests pass."

  - surface: Row System (family)
    canon: ["Vesper Row System.html", "row-spec.jsx", "row-system.jsx", "row-system-sections.jsx", "action-grammar-rows.jsx", "action-grammar.jsx"]
    code: ["components/ui/rows/", "components/trip-plan/PlanBlockRow.tsx", "components/ui/LedgerRow.tsx", "components/ui/ActivityLedgerRow.tsx"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11 (post-fix)
    status: partial
    notes: "Wave 1 re-audit (2026-07-11): D18 and D19 CONFIRMED closed with faithful canon documentation (row-system.jsx now carries explicit 'OVERLAYS (D18, canon 130)' and 'proposed (D19, canon 130) is deliberately the MERGE POINT' comment blocks) and matching code comments in PlanBlockRow.tsx. But the D18 implementation had a real bug: `failed` booking outcomes rendered a halo, contradicting canon's explicit 'expired/failed — NO halo' spec (only held_for_payment gets the urgency halo). FIXED same day, commit c7f20ada. Also: canon's 5-wrapper family (row-spec.jsx:196 — EditorialListRow/DateRailRow/TimelineObjectRow/ChangesLedgerRow/SettingsRow) has real RN implementations of the ledger-row wrappers living OUTSIDE this row's code: list (components/ui/LedgerRow.tsx, components/ui/ActivityLedgerRow.tsx, both correctly composing RowCore) — added above, mapping completeness fix, not a code defect. TimelineObjectRow.tsx's reduced 4-state grammar (vs the rich 8-state spec) remains a known, deliberately-deferred gap — 1 call site, mock data, open scope question. Token fidelity (leading-slot widths, row min-heights, divider-inset formula) confirmed exact against row-spec.jsx/row-system.jsx."

  - surface: Cards kit (family)
    canon: ["Vesper Cards.html", "Vesper Action Card Ownership.html", "vesper-cards-kit.jsx", "vesper-cards-system.jsx", "vesper-cards-families.jsx", "vesper-cards-fam.jsx"]
    code: ["components/vesper-cards/", "components/ui/CardSurface.tsx", "utils/vesperCardFamilies.ts", "components/focus-home/", "components/takes/", "components/concierge/"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11): six-family taxonomy + legacy-seven migration mapping, CardShell r14, and ReceiptFace/DecisionSheet anatomies all CONFIRMED exact. components/concierge/ (added Wave 0, unverified until now) checks out clean — VesperCardKit.tsx is a genuine re-export compatibility shim over utils/vesperCardFamilies.ts, not a competing kit, matching the Ownership doc's ruling. DeckCompareFace (previously flagged dead) is now actually deleted (arch-simp Tier 0 landed). BUT the prior note's D4 claim ('fixed transact/trust FAM ... to canon's exact D4 values') was only half right: Trust's spine color is off by one hex digit — code's sageDeep=#5E6E55 vs canon's #5E7355 (green channel 0x6E vs 0x73) — likely because a prior check compared against vesper-cards-families.jsx, which Vesper Action Card Ownership.html:305 explicitly marks non-canonical REFERENCE with a different palette than the real vesper-cards-kit.jsx FAM object. Two more real gaps: ActionRow never implements canon's dismiss-vs-navigational secondary-action styling split (DISMISS_RE regex in vesper-cards-kit.jsx has zero code counterpart — every secondary action renders identically); ConciergeCaptureNudgeCard hand-rolls its own card shell (r14 by coincidence, single-layer shadow not letterpress) instead of using CardSurface — the exact 'hand-rolled r9-13, no shadow' anti-pattern canon's own audit calls out for migration. Minor: Deck.tsx's r20 full-screen overlay is a 6th, unaudited shadow recipe outside canon's 5-container table (plausibly intentional for full-screen presence, undocumented)."

  - surface: Sheets (family)
    canon: ["Vesper Interaction Surfaces.html", "sheet-atoms.jsx", "sheet-header.jsx", "sheet-field.jsx", "field.jsx", "section-edit-sheet.jsx", "revert-conflict-sheet.jsx", "trip-record-sheet.jsx"]
    code: ["components/ui/BottomSheet.tsx", "components/ui/SheetHeader.tsx", "components/ui/SheetField.tsx", "components/ui/ActionListSheet.tsx", "components/ui/ConfirmDialog.tsx"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11): D21 (dense-size collision) CONFIRMED fully correct — SheetHeader.tsx's dense size (17px, typography.h1Dense = sans_semibold) and CostsBalanceSheet's migration (serif→sans title, verified via commit 5a875c2d diff) both match canon's sheet-header.jsx exactly. ~87% adoption figure sanity-checked, still roughly right. New findings beyond D21's scope: (1) canon's own reference implementation, SectionEditSheet.tsx, does NOT use SheetHeader's `edit` mode built specifically for it — instead stacks a default-anatomy SheetHeader (handle+20px title) on top of a second, hand-rolled Cancel/Save row, and routes its dirty-dismiss through the heavier centered ConfirmDialog rather than canon's bottom-sheet-anchored discard panel. `<SheetHeader edit` has zero call sites anywhere in the app — the edit chrome exists but was never adopted, not even by its namesake surface. (2) `large`/`edit` title sizes render at Medium weight (sans_medium/500) against canon's executable spec of SemiBold (600) for both — flagged for adjudication, not auto-fixed, since `edit` is currently unused and canon's own `large=600` blessing-comment never actually checked weight. (3) RevertConflictSheet.tsx is a ready, un-migrated `dense` candidate (specs 17px/600 titles, currently defaults to `large`/20px) — same pattern CostsBalanceSheet just fixed. (4) no code equivalent of canon's VSheetField (borderless read-only display atom, D3's other half) — read-only rows are hand-rolled per surface. Prior Batch E (CC4.1/4.2/4.5) work reconfirmed intact, not re-detailed here."

  - surface: State System (family)
    canon: ["Vesper State System.html", "vesper-state-system-app.jsx", "discover-feed-states.jsx", "reader-states-board.jsx", "changes-states-board.jsx", "session-recovery.jsx"]
    # components/ErrorBoundary.tsx re-homed here from Global Chrome (Wave 2,
    # 2026-07-11) — its non-dev fallback path defers to ErrorState, which is
    # this row's territory, not Global Chrome's (no canon coverage there).
    code: ["components/ui/state/", "components/ErrorBoundary.tsx"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11): removed trip-edge-states.jsx from this row's canon list — its content (Trip-Home hero/margin ranking, DT.* tokens) is unrelated to the empty/loading/error/offline taxonomy the rest of this row's files share (SS.*/T.*/DD.* tokens); its real home is the Trips-Home-hero surface, already tracked separately (project_trips_home_hero memory). Legacy EmptyState deletion and the ~57-importer figure CONFIRMED. D6's full-page hard-failure blessing is real in canon, but the 'one kit, code already had this right' framing was wrong: ErrorState.tsx and StateScreen.tsx(tone='failure') are two VISUALLY DIFFERENT implementations of the same blessed tier, live simultaneously — ErrorState (64px circular oxblood badge, pill CTA) at 34 call sites, StateScreen at 13+ (bare icon, no badge, StateAction CTA) — exactly the 'local state treatment' drift canon's own doc warns against. Two canon-specified experiences have zero code counterpart: Discover's cold-start chip-picker (discover-feed-states.jsx ScreenFeedColdStart — 'Tell Vesper what you're drawn to' + interest chips; DiscoverCoverHome collapses this into a generic empty state) and the reader's sparse-section InlineAbsence panel (reader-states-board.jsx ReaderStateSparse; the dossier reader has no such treatment). Substrate-gated: StateNotice's queued/syncFailed tones are fully built with zero call sites — no offline-write feature exists yet to trigger them. SessionRecovery and the Changes board both CONFIRMED near-verbatim matches, including a well-disclosed deliberate deviation (SessionRecoveryStaleNotice's icon+auto-dismiss vs canon's bare-dot mockup). Wave 3 housekeeping (2026-07-11): merged the standalone 'State System' row (duplicate — same code:, canon citing vesper-state-system-app.jsx, which this Wave-1 audit had already read and cited extensively, e.g. D6 at vesper-state-system-app.jsx:143) into this row and deleted the duplicate. No new content, just reconciling the manifest to match what was actually already verified."

  - surface: Tokens (family)
    canon: ["Vesper Design Tokens.html", "Vesper Productive Type & Material.html", "vesper-tokens.jsx", "token-reference-app.jsx", "design-system.jsx", "gouache.jsx"]
    code: ["constants/colors.ts", "constants/typography.ts", "constants/layout.ts", "constants/motion.ts", "constants/fonts.ts", "constants/cardSurface.ts"]
    aligned_at: 134
    code_verified_at: c7f20ada    # travel-app HEAD, Wave 1 re-audit 2026-07-11
    status: partial
    notes: "Wave 1 re-audit (2026-07-11) CONFIRMS the foundation: primitive paper/ink/gold hex values match canon byte-for-byte, motion durations/easing match exactly (canon's own header claims this and it checks out), font-family assets correct, base 4px spacing scale exact. The CC4.3/4.4/4.5 history below is real and holds up. Two live gaps survive despite that history: (1) muteSoft/ink80 is still used as real TEXT color at ~292 sites (discoverMeta metadata rows, trip-accommodations provider/confirmation text, tab-bar inactive label) despite canon's explicit 'DECORATIVE ONLY, never text' rule and FAIL-rated contrast — the muteSoft-budget ratchet caps growth but never drove this to zero. (2) `blue60`/`planningInk` (#3D5066, feeding Stay/Costs 'productive' surfaces) was never reconciled to canon's #2A5A8A the way its sibling status hues were — no CC4.3-style comment, the one primitive in the trio left unreconciled. Also found: `green60` primitive and most of `vesperSemanticTokens` are dead code (zero consumers) — hygiene, not a rendering bug. Prior CC4.3-4.5 history retained below:
      CC4.3 DONE (671a3d7d): status-hue trio snapped to canon ox60/sage60 — colors.danger + colors.surface.oxblood unified to #7A2E2E (were 2 different reds); olive[600] (the text-weight tone, not the fill-base olive[400]) → #2E5A3A; colors.success now references olive[600] directly. CC4.4 DONE (52da717a): 104 genuine-body-text sites migrated muteSoft→mute across 60 on-wedge files (30 correctly left as decorative, 5 flagged UNCLEAR) via a classification workflow reading actual JSX render sites, not just style-key names. CC4.5 DONE (Batch H, ff6d6eaa): scripts/check-muteSoft-budget.mjs wired into CI (baseline locked post-CC4.4, only ever decreases) so muteSoft-as-text can't silently regrow — caught Batch F's PlanBlockRow.tsx introducing 2 new muteSoft-as-text sites the same day (287→288), fixed same-session (6f6792c3, baseline retightened to 286). Durability now structural, not just a one-time count. D7 RESOLVED (canon 129): sub-9pt floor answered — a scoped fsStamp:8 exception is now canonized in vesper-tokens.jsx, permitted ONLY for mono/uppercase/letter-spaced(≥1.2) stamps (folio/trip date stamps, auth/receipt micro-labels). Code should add an fsStamp equivalent + point the sanctioned sub-9 sites at it, and the fontSize ratchet's PENDING sub-9 exemptions can be reclassified as canon-sanctioned rather than debt. OPEN: colors.surface.sage is OVERLOADED (status-affirmative in some places — InlineChip fill — pure editorial decoration in others — Places/Discover Fleuron/VesperSignature tints) — needs the same file-by-file judgment CC4.4 used before its value can move; do NOT blind-snap it. vesperSemanticTokens still mis-mapped/dead (zero consumers found — confirm delete vs revive). oxbloodDeep (#7C2F22) near-identical to the snapped oxblood — ramp-collapse question, out of scope. The 5 UNCLEAR CC4.4 sites + its 30 LEAVE calls could use a second human skim. CC4.5 durability SHIPPED 07-10 (ff6d6eaa): 3 adoption ratchets wired into ci.yml alongside the existing 6 — muteSoft-budget (287, locks in CC4.4's post-migration count so ink80-as-text can't regrow silently), sheet-bypass-budget (14 raw <Modal call sites outside BottomSheet/ConfirmDialog — Deck.tsx's known H2-5 gap deliberately left unexempted so it keeps surfacing), local-button-budget (7 files with a hand-rolled btn/button style that don't import ui/Button — verified by hand that files composing Button via a style-prop override, the sanctioned CC1.1 pattern, are excluded by construction, not miscounted as violations). This is CC4.5 tasks 2+3 from code-alignment-brief-CC4-sheets-tokens.md; task 1 (the 6 family rows themselves) was already done 07-09."

  - surface: Voice registers (family)
    canon: ["vesper-voice-registers.jsx", "immersion-register.jsx", "vesper-shared.jsx", "Vesper Productive Type & Material.html"]
    code: ["components/brand/VesperSignature.tsx", "components/brand/VesperAttribution.tsx", "components/brand/VesperNote.tsx", "components/brand/VesperMark.tsx"]
    aligned_at: 134
    code_verified_at: 0f5e8bd5    # travel-app HEAD, follow-up fixes 2026-07-11
    status: gap    # blocked on D23 — see design/adjudication-brief-2026-07-11-vesper-signature.md
    d_items: [D23]    # pending: VesperSignature's "+" vs D8 — needs a Claude Design ruling, not a code call
    notes: "Wave 1 re-audit (2026-07-11) FOUND A REAL STRUCTURAL GAP the prior 'fully ALIGNED, no known code gaps' verdict missed: VesperSignature.tsx — the shared primitive powering VesperAttribution AND VesperNote, ~30 real consumers across nearly the whole chat/notes/Discover/Places surface — renders a literal `+` character (VesperSignature.tsx, styles.plus), never touching VesperMark at all. Canon is explicit and was spot-verified directly: vesper-shared.jsx — 'A bare \"+\" or arrow is NOT the brand mark... never a bare \"+\" (D8).' A gold '+' IS sanctioned in canon, but only for a narrow, differently-named component (VesperVoiceLine, Stay/Costs empty/settled states only) — nowhere near VesperSignature's actual footprint. D22 (the onboarding Fleuron→VesperMark conversion, verified correct in its 2 named files: SeamDecline.tsx, app/onboarding.tsx) undersold the remaining scope — a live, non-onboarding attribution eyebrow (components/onboarding/DeferredDiaryGiftSheet.tsx, reached from in-trip chat.tsx, not onboarding) still renders the retired Fleuron, and several Discover/Places surfaces (DiscoverAtoms LensChip, DiscoverCoverHome, SpotTake) render Fleuron under explicitly Vesper-voice-coded colors (colors.discover.vesper). Also found: components/places/PlaceHome.tsx defines a LOCAL function literally named `VesperMark` that actually renders Fleuron — a naming collision that would produce false-positive greps in any future audit. Prior CC3 voice-hygiene work (italic-only-for-Vesper-voice discipline, 6 tasks, all independently re-confirmed accurate for what they covered) genuinely holds — VesperNote's roman/italic register split is correct. This is a scope gap in what got audited, not a regression in what was checked. Action: needs a real fix batch (VesperSignature → route through VesperMark or get its own D8 carve-out ruled explicitly) before this can move off `gap`; the DeferredDiaryGiftSheet/Discover Fleuron sites are smaller, mechanical follow-ups. Prior CC3 history retained for reference: core registers + Whisper-killed + D8/D9/D10 all shipped 07-10 across 6 tasks (c39101bd, d4bfd900, 1a67140d, 8dff50c5, 77b5d78b + mop-ups) — see git log for detail, not re-verified line-by-line this pass.
      FOLLOW-UP (2026-07-11, travel-app 0f5e8bd5): the small, mechanical half is DONE — DeferredDiaryGiftSheet.tsx's attribution eyebrow, DiscoverAtoms LensChip, DiscoverCoverHome's Vesper pill + 'ATLAS REMEMBERS' row, SpotTake's two direct-Vesper-CTA sites, and PlaceHome.tsx's naming-collision shadow function are all converted Fleuron→VesperMark (verified each site's color/context first — 3 sibling sites with content-category-driven, non-Vesper-exclusive coloring were deliberately left alone, matching D22's decorative-vs-attribution distinction). VesperSignature's own '+' — the structural half, ~30 consumers — is NOT fixed: it's a real design call, not a code call (VesperSignature is a deliberately built, actively maintained primitive that may simply predate D8 or need its own carve-out, the same shape as D22's onboarding exception). Adjudication brief drafted and ready: design/adjudication-brief-2026-07-11-vesper-signature.md (D23). This row stays `gap` until D23 is ruled."
