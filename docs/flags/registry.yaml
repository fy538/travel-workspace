# Feature-flag registry — canonical owner/expiry source for both repos.
#
# Why this exists: as of 2026-07-06, 26 flags across travel-agent and
# travel-app had zero owner or expiration metadata anywhere in the code.
# Untracked flags are exactly how "shipped dark for now" becomes permanent
# debt (see docs/working/ research notes on flag-lifecycle management).
# This file is the single source of truth; scripts/check_flag_registry.py
# enforces it at pre-push (see workspace .pre-commit-config.yaml).
#
# Fields:
#   name      - env var / const name as it appears in code
#   repo      - travel-agent | travel-app
#   location  - file:line or symbol where the flag is defined
#   default   - the flag's default value today
#   category  - release   = a feature rollout, expected to go away once shipped
#               experiment = a trial/eval gate, expected to resolve one way or another
#               ops        = a permanent operational kill-switch/toggle, not a
#                            feature rollout — `expires` reads as "review by",
#                            not "must remove by"
#   owner     - accountable person (fy538 for all, per 2026-07-06 decision)
#   expires   - review/removal-by date. Fixed 90-day convention from creation
#               of this registry (2026-07-06 -> 2026-10-04) for every flag,
#               applied uniformly per instruction; category above tells you
#               how to *read* a blown date, not whether the check fires.
#   status    - active | resolved. check_flag_registry.py only fails on
#               `active` entries whose `expires` has passed. Once a flag is
#               actually removed or permanently flipped, set status: resolved
#               rather than deleting the row (keeps a historical record).
#   target_journey - proof/journey this flag is intended to advance (for
#                    example, P01 or P03); required for new thesis-slice flags
#   cohort    - smallest cohort allowed to exercise the flag today
#   kill_switch - exact safe-off behavior, including any prerequisite gate
#   promotion_removal_question - dated question that decides whether the flag
#                                broadens, stays dark, or is removed
#   notes     - short context, e.g. what has to happen before flip/removal

flags:
  # ── travel-agent (backend) ──────────────────────────────────────────────
  - name: SPATIAL_REACHABILITY_NEARBY_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:37
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Enables the bounded nearby reachability/isochrone canary after cache and provider validation.

  - name: NEARBY_TRAVEL_TIME_RANKING_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:49
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Ranks discover_nearby_ranked candidates by real Mapbox Matrix travel time instead of straight-line distance; fails open to the existing distance score per-candidate on any provider/response issue.

  - name: AMBIENT_ATTENTION_DISPATCH_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:120
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Keeps ambient attention dispatch dark while shadow-cycle receipts are evaluated.

  - name: AMBIENT_COINCIDENCE_CANDIDATES_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:126
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates candidate generation until ambient coincidence quality and consent boundaries are reviewed.

  - name: HOME_AGENT_LOOP_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:132
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Enables the bounded Home composition loop only after shadow parity and device review.

  - name: PLACES_EXPOSURE_ROTATION_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:85
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Enables bounded section rotation while the Places exposure policy is evaluated.

  - name: CONCIERGE_EXPOSURE_FATIGUE_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:96
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Enables the bounded concierge exposure-fatigue penalty during ranking evaluation.

  - name: CROSS_SURFACE_ECHO_DEFERRAL_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:109
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Defers repeated cross-surface content while its presentation policy is evaluated.

  - name: PLANNING_CONTEXT_SNAPSHOT_PARITY_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:283
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Runs snapshot parity checks before the planning-context read path changes.

  - name: PLANNING_CONTEXT_SNAPSHOT_CUTOVER_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:294
    default: false
    category: release
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates the planning-context snapshot read-path cutover after parity validation.

  - name: PLACES_SECTIONS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:131
    default: true
    category: release
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Default-on Places feed route; set false for an immediate rollback to the established Places projection.

  - name: PENDING_CHAT_TURNS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:pending_chat_turns_enabled
    default: false
    category: release
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates the durable pending-turn transport until the mobile outbox adopts it; default-off prevents an unproven alternate chat writer from being reachable.

  - name: PLACES_SECTIONS_FEED_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:25
    default: true
    category: release
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Default-on Places section-feed renderer; requires backend PLACES_SECTIONS_ENABLED and can be flipped false for immediate client rollback.

  - name: ATLAS_LLM_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:20
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates the Atlas real-LLM composer; flip once validated against the eval framework.

  - name: ANNIVERSARY_PUSH_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:30
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates the "On this day" anniversary push loop; flip once anniversary content validated at scale.

  - name: UNPACKED_SEASONAL_PUSH_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:40
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates the year-end "unpack your year" seasonal push loop; only acts within the year-end window regardless.

  - name: ATLAS_AUTO_CANDIDATE_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:50
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Trip-to-Atlas-candidate backfill safety net for missed trip.completed events.

  - name: ATLAS_SIGNALS_TO_MEMORY
    repo: travel-agent
    location: backend/core/feature_flags.py:60
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Feeds Atlas derived_signals into concierge taste model; gates cross-trip affinity recall (J17 depends on this).

  - name: AUTO_PUBLISH_GREEN_DOSSIERS
    repo: travel-agent
    location: backend/core/feature_flags.py:70
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Auto-promotes green-quality research dossiers to approved; decide publish posture (see project_research_pipeline_pilot memory).

  - name: STORY_SHARING_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:96
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates outward story/Unpacked share-link minting; flip once J19 public-taste privacy checks certified.

  - name: VENUE_DISRUPTION_PROPOSALS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:112
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: >
      Proactive venue-closed swap-proposal producer — the concrete
      Advise-vs-Act gap: the agent still only proposes (human
      approves/reverts), but this is the first step toward the agent
      initiating that proposal itself rather than waiting on a user tool
      call. Explicit decision 2026-07-06: stays dark for dogfood cohort 1.
      The cohort validates the invite/group-planning loop (belief #14),
      which doesn't need proactive agent-initiated proposals — flipping an
      unevaluated proactive surface on for a real friend group's first
      impression risks the same fake-personalization failure mode design
      canon audits keep flagging. Evaluate against real cohort-1 usage
      data, then flip for cohort 2 if the eval passes — that evaluation is
      the next explicit decision, not this flag staying dark indefinitely.

  - name: PLANNER_SEMANTIC_RANKING
    repo: travel-agent
    location: backend/core/feature_flags.py:170
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-25
    status: active
    notes: Enables Qdrant ranking only within SQL-approved planner venue candidates while corpus coverage is evaluated.

  - name: ITINERARY_OPERATIONS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:164
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Global fail-closed kill switch for canonical itinerary writes; reads and previews remain available when disabled.

  - name: CONTEXT_COMPILER_PLANNER_PARITY_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:164
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-17
    status: active
    notes: Dual-renders the typed planner context for content-free parity telemetry before cutover.

  - name: CONTEXT_COMPILER_PLANNER_CUTOVER_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:170
    default: false
    category: release
    owner: fy538
    expires: 2026-10-17
    status: active
    notes: Selects the compiled typed planner overlay only after membership and coverage checks pass.

  - name: DOSSIER_APPROVED_FILTER_CHAT
    repo: travel-agent
    location: backend/core/settings.py:158
    default: true
    category: release
    owner: fy538
    expires: 2026-10-04
    status: resolved
    notes: Flipped on 2026-07-06, prod coverage ~99.8%. Marked resolved; keep the row for history.

  - name: POSTCARD_RENDER_ENABLED
    repo: travel-agent
    location: backend/core/settings.py:170
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Riso postcard rendering (homecoming v3) — ships dark, wired end-to-end, pending social-distribution cert.

  - name: CONTENT_GRAPH_GEOFENCE_ENABLED
    repo: travel-agent
    location: backend/concierge/config.py:121
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Off-itinerary geofence lookup for the content graph (Phase 3A); off by default for per-turn DB cost.

  - name: CONCIERGE_SEARCH_TRANSPORT_ENABLED
    repo: travel-agent
    location: backend/concierge/config.py:132
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Layer 3 of the transport product (live Amadeus/Duffel/Rome2Rio); tool stays wired but returns a polite stub when off.

  - name: BOOKING_DUFFEL_LIVE_BOOKING_ENABLED
    repo: travel-agent
    location: backend/booking_agent/config/settings.py:15
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: >
      Gates live Duffel transaction execution (BE half of the booking gate;
      see EXPO_PUBLIC_LIVE_BOOKING_ENABLED for the FE half). NOT an open
      decision — deliberately deferred per the generated V1 release contract
      (the original 2026-06-30 record is archived): "Booking: transaction engine
      (Duffel/cart/hold) OUT/flagged; keep a non-transacting 'mark as
      booked' + external deep-link stub IN." Reaffirmed 2026-07-06 — no
      product need to flip this before the dogfood cohort, which validates
      the invite/group-planning loop, not the booking completion path.

  - name: EXPO_PUBLIC_LIVE_BOOKING_ENABLED
    repo: travel-app
    location: "app/booking/[sessionId].tsx:656 (isLiveBookingEnabled())"
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: >
      FE half of the booking gate (paired with backend
      BOOKING_DUFFEL_LIVE_BOOKING_ENABLED). Added to this trust-loop fix
      2026-05 so the "AI books directly" label doesn't imply a committed
      order before one exists — shows the honest "handoff ready" label
      while off. Missed in the original 2026-07-06 flag inventory because
      it's an inline process.env read, not in constants/featureFlags.ts;
      added retroactively when reconciling the booking-flag decision. Same
      deliberate-defer reasoning as the BE flag above.

  - name: BOOKING_MONETIZATION_MODE
    repo: travel-agent
    location: backend/booking_agent/config/settings.py:37
    default: free_affiliate
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Not a bool toggle — a mode string. Review whether affiliate config (pid/mcid) should be populated before MVP.

  - name: LOOKUP_SYNTHESIS_ENABLED
    repo: travel-agent
    location: backend/lookup_agent/config/settings.py:36
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: On by default; review whether this should stay a flag or be hard-wired.

  - name: DISABLE_LLM_BACKGROUND_LOOPS
    repo: travel-agent
    location: backend/background_config.py:46
    default: false
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Kill switch (background loops run unless disabled). Permanent by design — expires date is a review checkpoint, not a removal deadline.

  - name: DISABLE_API_BACKGROUND_TASKS
    repo: travel-agent
    location: backend/background_config.py:56
    default: false
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Kill switch, same shape as DISABLE_LLM_BACKGROUND_LOOPS. Permanent by design.

  - name: TELEMETRY_DISABLED
    repo: travel-agent
    location: backend/telemetry.py:221
    default: false
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Telemetry kill switch. Permanent by design — expires date is a review checkpoint.

  - name: LANGFUSE_ENABLED
    repo: travel-agent
    location: backend/langfuse_tracing.py:144
    default: true (if keys present)
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Tracing toggle, effectively on whenever keys are configured. Permanent by design.

  - name: LANGFUSE_CAPTURE_USER_CONTENT
    repo: travel-agent
    location: backend/langfuse_tracing.py:58
    default: false
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Privacy-sensitive — controls whether raw user content is captured in traces. Review before ever flipping true.

  - name: EXPO_PUSH_ENABLED
    repo: travel-agent
    location: backend/notifications/receipt_reaper.py:39
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates outbound Expo push sends from the receipt reaper loop.

  - name: EMBEDDING_PREWARM_ENABLED
    repo: travel-agent
    location: backend/api/lifecycle.py:223
    default: false
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates startup embedding-model prewarm; disabled by default to keep boot independent of model availability.

  # ── Found unregistered by scripts/check_flag_registry.py's ad-hoc-lever
  # scan (2026-07-10, simplification audit). All 8 are read via
  # truthy_env/falsy_env rather than the Pydantic-Settings convention the
  # rest of this file mostly documents — see that script's module
  # docstring for why this convention was the blind spot.
  - name: ATLAS_SEMANTIC_READ
    repo: travel-agent
    location: backend/search/dispatch.py:672
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates Qdrant dense-semantic Atlas search vs the pg_trgm fallback. Paired with ATLAS_SEMANTIC_WRITE.

  - name: ATLAS_SEMANTIC_WRITE
    repo: travel-agent
    location: backend/core/db/atlas.py:276
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates fire-and-forget Qdrant upsert/delete on Atlas writes. Paired with ATLAS_SEMANTIC_READ — must ship together (write without read never gets exercised; read without write serves a stale/empty index).

  - name: FACTS_WRAPPER_ENFORCE
    repo: travel-agent
    location: backend/core/llm.py:313
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: M1 facts-wrapper ships dark (scan + log only); this escalates to inject/clamp. Data-gated M2 rollout per design canon, never a launch flip — see project_facts_primitive memory.

  - name: PLANNING_ITINERARY_FIRST
    repo: travel-agent
    location: backend/planning_agent/schemas.py:179 (schema default); production default set via backend/concierge/tool_handlers/planning/_plan.py:74
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: >
      Planning-cost lever 4 (itinerary-first synthesis split). Schema
      default is OFF (truthy_env) so eval/notebook/direct-construction
      callers are unaffected; production defaults it ON via a separate
      falsy_env derivation in _plan.py — the real prod value is "on unless
      explicitly killed." Live-validated 2026-07-09 (privacy hold + quality
      floor pass, docs/audits/planning-cost-latency-2026-07.md). Kill
      switch: PLANNING_ITINERARY_FIRST=false.

  - name: PLANNING_PREFETCH
    repo: travel-agent
    location: backend/planning_agent/schemas.py:199 (schema default); production default set via backend/concierge/tool_handlers/planning/_plan.py:84
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Planning-cost lever 1 (deterministic prefetch sweep). Same schema-off/prod-on shape as PLANNING_ITINERARY_FIRST. Live A/B trending positive but noisy (n=2) — revisit with more reps. Kill switch — PLANNING_PREFETCH=false.

  - name: PLANNING_REPLAN_DIFF
    repo: travel-agent
    location: backend/planning_agent/schemas.py:190 (schema default); production default set via backend/concierge/tool_handlers/planning/_plan.py:69
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Planning-cost lever 3 (diff-only replan). Same schema-off/prod-on shape as PLANNING_ITINERARY_FIRST. Live A/B measured 2026-07-09 at -82% wall-clock, -84% cost — the cleanest win of the six levers. Kill switch — PLANNING_REPLAN_DIFF=false.

  - name: PLANNING_STRUCTURED_OUTPUT
    repo: travel-agent
    location: backend/planning_agent/schemas.py:213 (schema default); production default set via backend/concierge/tool_handlers/planning/_plan.py:79
    default: true
    category: ops
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Planning-cost lever 2 (schema-constrained synthesis call). Same schema-off/prod-on shape as PLANNING_ITINERARY_FIRST. Live A/B measured 2026-07-09 at -16% cost but +16% wall-clock; flipped on anyway by explicit product decision. Kill switch — PLANNING_STRUCTURED_OUTPUT=false.

  - name: PLANNING_TERSE_REASONING
    repo: travel-agent
    location: backend/planning_agent/schemas.py:163
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Latency prototype — shrinks the final synthesis reasoning_trace (the slowest single call, ~50% of wall-clock). Off by default; env-flippable for A/B measurement only, not yet a product decision.

  - name: SHARED_WORKFLOW_CONTROL_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:78
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Enables CAS-fenced workflow command recording only; command application and live authority remain disabled until the shared-control canary and device recovery review pass.

  - name: SOCIAL_CIRCLE_AGENT_CONTEXT_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:53
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates bounded, explicit-circle facts entering shared-agent context while privacy and device evidence are reviewed.

  - name: GROUP_ATTENDANCE_CONTEXT_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:65
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates aggregate attendance counts entering shared-agent context; individual choices and notes remain excluded.

  - name: WEATHER_RESCUE_PROPOSALS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:274
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: P07
    cohort: exact UUIDs in WEATHER_RESCUE_TRIP_IDS while the global flag is explicitly enabled
    kill_switch: unset or false, or an empty WEATHER_RESCUE_TRIP_IDS allowlist, prevents weather rescue proposal production while preserving ordinary Plan reads
    promotion_removal_question: By 2026-10-30, did grounded rescue, canonical lifecycle, privacy, and two-observer device evidence justify promotion?
    notes: Gates current-weather rescue proposal production until freshness, group-safe composition, canonical apply, and two-observer device evidence pass. Malformed allowlist values are ignored and an empty cohort fails closed.

  - name: GUEST_PROPOSAL_CAPABILITIES_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:278
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    notes: Gates one-use, purpose-bound proposal guest links and authenticated membership upgrade until mobile handoff, canonical vote continuity, and two-observer device evidence pass.

  - name: VOICE_ENABLED
    repo: travel-app
    location: utils/voiceEnabled.ts:12
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Gates voice/LiveKit UI entry points; needs backend voice secrets configured first (returns 503 otherwise).

  - name: POSTCARDS_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:11
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Ships after social-distribution cert. Paired with backend POSTCARD_RENDER_ENABLED.

  - name: AMBIENT_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:14
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Live-ambient feed cards on trips home. Ships after ambient eval.

  - name: LOCAL_PLAN_DOGFOOD_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:25
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: P01
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_LOCAL_PLAN_DOGFOOD_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: >
      By 2026-10-30, does the local-occasion doorway have current-revision
      seeded and device proof beyond the fixed Friday-night seed, or should
      this internal gate be removed with the unfinished doorway?
    notes: >
      Internal-only local Plan activation. Keep dark until the occasion-to-Plan
      loop is device-certified; OUTCOME_ARTIFACT_ENABLED remains downstream.

  - name: TRIP_EDITORIAL_MAP_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:32
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J08
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_TRIP_EDITORIAL_MAP_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: >
      By 2026-10-30, does the editorial Day Map have map/query parity and
      current-revision device evidence for live-trip context, or should the
      route and its flag be removed?
    notes: >
      Internal-only Trips Home editorial map gate. The query and membership
      path must remain dark together until map/time truth is certified.

  - name: TRIPS_NEAR_YOU_DOGFOOD_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:47
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J08
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_TRIPS_NEAR_YOU_DOGFOOD_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: By 2026-10-30, does the foreground near-you receipt have current location, map, and device proof, or should this dark entry be removed?
    notes: Internal-only foreground location receipt; the client never invents a nearby result.

  - name: PLACES_READING_SPINE_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:59
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J07
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_PLACES_READING_SPINE_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: By 2026-10-30, does the mirrored reading treatment have visual and accessibility evidence, or should it remain absent?
    notes: Presentation-only exploratory Places treatment; it introduces no new content authority.

  - name: PLACES_SAVED_UNPLACED_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:70
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J07
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_PLACES_SAVED_UNPLACED_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: By 2026-10-30, does the owner-only saved-unplaced projection earn device evidence with its backend source, or should both gates be removed?
    notes: Client presentation gate paired with the backend Saved Unplaced producer.

  - name: TRIPS_SAVED_UNPLACED_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:194
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J08
    cohort: internal-founder-dogfood
    kill_switch: Set TRIPS_SAVED_UNPLACED_ENABLED=false; Trips Home omits the owner-only saved-unplaced module.
    promotion_removal_question: By 2026-10-30, does the Trips Home projection earn device evidence without implying a recommendation or visit, or should the gate be removed?
    notes: Backend producer gate for the owner-only Trips Home Saved Unplaced module; separate from the Places presentation gate.

  - name: TRIP_FEEL_STATIC_EXPLORATION_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:81
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J08
    cohort: internal-founder-dogfood
    kill_switch: Set EXPO_PUBLIC_TRIP_FEEL_STATIC_EXPLORATION_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: By 2026-10-30, did the static visual exploration earn a server-owned fact contract, or should the exploratory surface be removed?
    notes: Internal visual exploration only; it has no write, telemetry, or conversational authority.

  - name: PLACES_REGISTERS_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:141
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J07
    cohort: internal-founder-dogfood
    kill_switch: Unset PLACES_REGISTERS_ENABLED or set it false.
    promotion_removal_question: By 2026-10-30, do evidence-first registers have source, privacy, and device proof, or should the adapter remain dark?
    notes: Backend gate for source-backed Places register exploration; it grants no recommendation or mutation authority.

  - name: PLACES_TWO_PLACE_COMPARISON_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:152
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: J07
    cohort: internal-founder-dogfood
    kill_switch: Unset PLACES_TWO_PLACE_COMPARISON_ENABLED or set it false.
    promotion_removal_question: By 2026-10-30, does the comparison adapter have durable candidate-set provenance, or should it remain unreachable?
    notes: Dark catalog comparison adapter; a flag alone never grants Places feed membership.

  - name: GROUP_TRIP_MICRO_JOURNEY_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:GROUP_TRIP_MICRO_JOURNEY_ENABLED
    default: false
    category: experiment
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P05,P07
    cohort: internal builds with an explicit app flag only
    kill_switch: Set EXPO_PUBLIC_GROUP_TRIP_MICRO_JOURNEY_ENABLED=false; the gate also requires INTERNAL_BUILD=true.
    promotion_removal_question: By 2026-09-09, did the bounded doorway and fixed Lisbon rescue earn controlled-device and staging proof, or should the doorway remain dark or be removed?
    notes: Opens only a private review-first handoff grounded in the canonical Plan; it grants no client mutation, push, group-output, or inferred-memory authority.

  - name: OUTCOME_ARTIFACT_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:39
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-30
    status: active
    target_journey: P03
    cohort: internal-founder-dogfood
    kill_switch: >
      Set EXPO_PUBLIC_OUTCOME_ARTIFACT_ENABLED=false; the app also requires
      LOCAL_PLAN_DOGFOOD_ENABLED=true, so disabling the parent gate is a full
      upstream kill switch.
    promotion_removal_question: >
      By 2026-10-30, does a confirmed local occasion produce a private,
      correctable outcome artifact with device proof, or should this surface be
      removed until its contract is ready?
    notes: >
      Downstream private post-occasion artifact. It cannot out-run the local
      Plan loop that produces and confirms its occurrence.

  - name: STORY_SHARE_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:17
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: Story + Atlas Unpacked share links. Paired with backend STORY_SHARING_ENABLED. Ships after J19 privacy cert.

  - name: DOSSIER_VOICE_STUB
    repo: travel-app
    location: constants/featureFlags.ts:20
    default: false
    category: release
    owner: fy538
    expires: 2026-10-04
    status: active
    notes: 'The "Hear the read" icon in dossier chrome — stub until TTS ships.'

  - name: AUTH_PHONE_ENABLED
    repo: travel-app
    location: constants/featureFlags.ts:23
    default: true
    category: release
    owner: fy538
    expires: 2026-10-11
    status: active
    notes: FE gate for phone-number auth screens. Registered 2026-07-17 during interaction-audit registry cleanup (was live in code, missing from registry).

  - name: CONTEXT_COMPILER_SHADOW_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:151
    default: false
    category: experiment
    owner: fy538
    expires: 2026-10-11
    status: active
    notes: Telemetry-only shadow comparison for the context compiler; grants no runtime authority. Registered 2026-07-17 during interaction-audit registry cleanup.

  - name: AI_DECISION_SHADOW_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:ai_decision_shadow_enabled
    default: false
    category: experiment
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P07
    cohort: none until explicitly enabled with the private disruption gate and exact Trip allowlist
    kill_switch: unset or false disables every AI-DL shadow attempt without changing the existing serving path
    promotion_removal_question: By 2026-09-09, did adjudicated offline evidence and approved telemetry controls justify bounded private shadow observation?
    notes: Global AI-DL shadow kill switch. It grants no visible response, mutation, notification, group-output, or memory-write authority.

  - name: AI_DECISION_SHADOW_PRIVATE_DISRUPTION_ENABLED
    repo: travel-agent
    location: backend/core/feature_flags.py:ai_decision_shadow_private_disruption_enabled
    default: false
    category: experiment
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P07
    cohort: exact allowlisted private Trip concierge turns only
    kill_switch: unset or false disables the private disruption policy even when the global shadow switch is enabled
    promotion_removal_question: By 2026-09-09, did the private disruption policy beat or complement the deterministic baseline without hard-gate regression?
    notes: Policy-specific gate for ask/show/recommend/abstain shadow decisions; proposal and execution remain forbidden.

  - name: AI_DECISION_SHADOW_TRIP_IDS
    repo: travel-agent
    location: backend/core/feature_flags.py:ai_decision_shadow_trip_ids
    default: empty
    category: experiment
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P07
    cohort: explicit comma-separated Trip UUID allowlist; empty means no eligible Trips
    kill_switch: clear the value to make the eligible cohort empty
    promotion_removal_question: By 2026-09-09, is an exact Trip allowlist still required or should the entire shadow experiment be stopped?
    notes: Malformed UUIDs are ignored; the setting never grants product or mutation authority.

  - name: AI_DECISION_SHADOW_TIMEOUT_MS
    repo: travel-agent
    location: backend/core/feature_flags.py:ai_decision_shadow_timeout_seconds
    default: 250
    category: ops
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P07
    cohort: enabled private disruption shadow attempts only
    kill_switch: set the global AI_DECISION_SHADOW_ENABLED switch false; timeout is clamped to 25-1000 ms
    promotion_removal_question: By 2026-09-09, do observed latency and timeout rates justify retaining this ceiling?
    notes: Operational ceiling for a fail-open observer; timeout cannot block or replace the visible serving path.

  - name: AI_DECISION_SHADOW_MAX_COST_MICRO_USD
    repo: travel-agent
    location: backend/core/ai_decision_shadow.py:ai_decision_shadow_max_cost_micro_usd
    default: 0
    category: ops
    owner: fy538
    expires: 2026-09-09
    status: active
    target_journey: P07
    cohort: enabled private disruption shadow attempts only
    kill_switch: zero prevents provider-backed shadow execution; the global shadow switch remains the primary off control
    promotion_removal_question: By 2026-09-09, is any nonzero provider budget justified by the frozen comparison protocol?
    notes: Cost ceiling for private shadow observation; no delivery, mutation, group, or memory cost is authorized.
