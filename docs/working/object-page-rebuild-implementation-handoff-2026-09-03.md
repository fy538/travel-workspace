---
doc_type: working
status: active
owner: founder / product / design / engineering
created: 2026-09-03
last_verified: 2026-09-15
expires: 2026-10-03
why_new: Preserves the object-page design handoff and its unresolved engineering decisions without promoting design fixtures into canonical runtime authority.
source_of_truth_for: []
supersedes: []
---

# Object Page Rebuild — Implementation Handoff (2026-09-03, rev 6)

## Current coverage assignment — September 12

**Preserve Entity 14 R2's purpose-responsive composition**, including the
“OPENED FOR SATURDAY” setup in workbench 02C. The
[September 11 snapshot](../design-archive/design-language-2026-09-11/README.md)
preserves accepted adoption. No invariant-body redesign, place-first replacement
for R4 originals or unnecessary 0.4.1 migration. The older candidate brief is
supporting constraints, not a second design queue.

**One requested-work/return continuation:**

1. Candidate/known identity → selected sparse or purpose-responsive destination.
   Carry the resolved purpose; use existing genuine-ambiguity variants only
   when needed, not another question about an already named target.
2. Explicit research/check → accepted/working → leave → result, failure or
   stale state → originating field. Older board 13 and code already contain
   research-state concepts. Finish the selected shared failed-check specimen
   and reachable result, not a new engine or generation triggered by a read/GET.
3. Keep the exact attributed human original independently reachable. External
   practical/provider doors preserve origin and current state; opening a
   provider proves neither booking nor completion.

Home receives meaningful later results; Places owns the field; Chat the
question; backend owners determine supported work. No research dashboard,
catalog backfill or provider checkout.

**Handback:** selected entry/control/result/return, donor references, truthful
failure and service dependencies. Static labels do not prove execution or
freshness. Follow [shared §23](vesper-shared-design-language-consolidation-2026-09-10.md#23-september-12-coverage-follow-through).
This design update neither authorizes production edits nor restarts Integration.

## Requested-work continuation delivered — September 15

**Board 16 in the Entity lab (dd48304b); drawn design evidence only.** The
September 12 continuation is drawn end to end on one place, in twelve frames on
the adopted shell (`vdl-stage1 0.3`; no 0.4.1 migration).

- **Entry, purpose carried.** A Places row carrying Saturday opens either
  destination: the selected one (14 R2, shown as a live crop of board 14 itself)
  or the sparse one (06B's shell plus one new context line). Neither re-asks the
  named target; 10.3B remains the only frame that asks, and only when nothing
  was named.
- **Requested work.** The check is explicit and starts from the hours cell's own
  link — nothing runs on opening. Six states follow, each with what the page
  says, what the originating row says, and what neither may say: asked, running
  (Notice `pending`, Cancel only), result (`applied`, with source and as-of),
  known failure (`failed`, the only tone offering a retry), unknown outcome
  (`unknown`, retry held until reconciled, no "nothing changed"), and stale
  (`stale`, the value kept with its age and re-check offered once). Leaving
  never cancels and never blocks navigation.
- **Originals and provider doors.** Dana's note is reachable on its own through
  the shared reader at the open density; opening is not using. The provider door
  is 12.2, shown as a live crop of board 12 itself, and the return states the
  non-event: the table cell still reads Reserve, the arrangement still reads an
  option, and only a supplied confirmation changes either (12.3).
- **Behaviour donors, referenced not redrawn:** Plans 90 J2c, J2d, J2e for
  pending/failed/unknown, and D5 and D7 for the provider return and the stale
  alternative.
- **Dependencies named, not drawn into existence:** a check service that reports
  its own state including unknown; an as-of on the fact itself; Social and Life
  keeping the original retrievable and withdrawable; the approved-link policy.
- **Not built:** any research engine, catalog backfill, status dashboard or
  checkout; research on a read; a claimed confirmation; a new chooser family.

Board 00 indexes 16. Board 14 R2 is unchanged, and no board changed in the
September 11 snapshot.

**Evidence boundary.** Served bytes for both written files equal the generated
sources apart from host whitespace; the served render of 16 matches the local
copy across 823 elements with 0 failed requests and 0 page errors, the only
difference being a 15px height on the live crop of 14; both live crops mount and
show their named frame. Drawn design only: nothing here is selected,
implemented, verified behaviour, or support for a fixture claim. The open items
above keep their owners.

## Additive review — September 10: preserve the open queue

**This adds to the existing assignment and coverage work; it does not replace
them.** No current item is removed or marked complete. Verify exact selected
frames, their continuations and consumers before closing a demonstrated portion
of a request. The existing Entity 14 selection remains decided, while its
unfinished reconciliation and evidence work remain in scope.

### Additional clarification: do not ask for an already named target

In **10, situation 3**, the person asks, “Would this work for Maya's dinner?”
The next “Which one?” chooser offers Maya's Saturday and Theo's Friday even
though the request has already named its target.

Carry Maya's dinner into the useful assessment. The answer may end privately;
asking about fit is not itself an instruction to edit or share. If the person
chooses a consequential continuation, preview that named arrangement and its
actual audience. Ask for a target only when consequential ambiguity remains,
such as two genuinely plausible dinners with Maya—not merely because more
than one arrangement exists.

Apply the existing rule in
[Plans §0.7](claude-design-plans-in-real-life-handoff-2026-09-04.md#07-post-subtraction-refinement-simplify-the-machinery-preserve-the-help):
one resolved target and the smallest necessary continuation. Extend Entity's
existing finite-target coverage with **resolved target / genuine ambiguity**;
do not create a new chooser family or another step before every private answer.
Preserve the originating object, words and return position.

### Existing work remains open where not demonstrated

The **09 invariant-body instruction versus selected 14** conflict is already
assigned in residuals 1 and 6. Carry it into the existing selection-map
reconciliation rather than launching another purpose-responsive-body decision.
Keep exact-original access, support for fixture claims, sparse/requested-check
states, external-link treatment and larger-text/return evidence with their
current owners. Neither a selected body nor a drawn button completes those
continuations.

**Delivery:** add the corrected situation-3 trace and the genuine-ambiguity
variant reference to the existing handback. Supply it through the
[updated shared Stage 1 sequence](vesper-shared-design-language-consolidation-2026-09-10.md#154-revised-stage-1-order),
not an independent project redesign. No production behavior, new permission or
arrangement mutation is authorized by this design addition.

## Shared package adoption — September 11

**Independent review: accepted at the design-reference level.** The selected
R2 composition and the distinct R3/R4 entrances survive adoption. The localized
oxblood failure Notice on 05.6 is acceptable; no additional founder color
decision is needed. Keep the missing-variant ledger and selected local regions;
do not force R4 or non-photo openings into unsupported component shapes.
The [shared review and next assignment](vesper-shared-design-language-consolidation-2026-09-10.md#21-first-adoption-review--entity-and-social-september-11)
routes Social's reader correction to the shared-library owner. Entity may
consume relevant extensions later; it does not need to repeat this adoption
round or await every extension. Existing product/support/behavior work stays open.

**Delivered in the Entity lab (dd48304b); design evidence only.** The lab now
uses `vdl-stage1 0.3`, copied byte-for-byte from workbench c13ae951 (kernel
stamp `travel-app@e2e792913 tokens:80d0648dd300`). Boards 03, 05, 06, 06B, 09,
10, 11, 12 and 14 rebuild their page-shell regions from PlaceHead, FactPair,
SourceList, ActionGroup, LocationFooter and Notice wherever an equivalent exists,
with the reading's source/person marks from `vdl.css`. Every other region stays
as drawn. Lab-wide CSS moved to `entity-lab.css`; its colours now point at
kernel tokens, and labels below 10px were raised to the 10px floor. Each changed
board keeps its original, unmodified, under `before/`. Board 15 records every
frame (shared vs drawn), the missing variants and live before/after crops;
board 00 indexes 14 and 15. Boards 01, 02, 04, 07, 08, 13 and Z are unchanged.

- **Situation 3 corrected (10):** the "Which one?" chooser is gone. The answer
  carries Maya's dinner, may end privately, and previews only that named
  arrangement and its actual audience. **3B** is the genuine-ambiguity variant
  reference: the question names no evening and Saturday and Friday have different
  audiences, so the answer asks once, in line; after the choice, situation 3
  follows.
- **14 R2 preserved** in the same composition. The where row on R1–R3 now shows
  locality (`Sorrento [fixture locality]`), as requested. The street address
  still appears in the where rows of 03, 06, 09, 11 and 12.
- **Missing variants, reported rather than built locally:**
  - PlaceHead has no no-plate, map-plate or spot opening.
  - PlaceHead has no provider-photo credit slot.
  - PlaceHead has no standalone byline.
  - FactPair takes a link only at the start of the second cell; the lab also puts
    one in the first cell or after a value.
  - LocationFooter's map is fixed to R2's Sorrento geometry.
  - OriginalReader has no direct-original entrance for 14 R4.
  - There is no text-scale variant for 06B.4.
- **Stays project-specific (drawn):**
  - sheets (06.4, 12.4) and the Tonight card (06.5);
  - keep receipts and chips (10.1–2) and the directions/reserve receipts (12.1–2);
  - the arrival line (06B.2), candidate lists (05) and origin frames (09);
  - the Ask conversation (10.3, 3B) and the Life frames (11 A, C);
  - R5, which is owned by Places.
- **Reviewed tone change:** 05.6 uses the shared Notice `failed` tone
  (oxblood), accepted above as a localized failure treatment.

**Evidence boundary:**

- **Bytes:** served bytes for all 11 written files equal the generated sources.
  The only differences are host whitespace.
- **Render:** each served render matches the local copy element for element,
  with 0 failed requests and 0 page errors.
- **Text:** across 43 page-shell frames, the text changed only where intended.
- **Board 15 crops:** all 23 live crops mount in served context, and each
  shows its named frame.

Nothing here verifies native rendering, the Claude Design editor view, behaviour
or support for fixture claims. The source/support, exact-original,
requested-check and coverage items above remain open with their current owners.

## Shared design-language workstream — September 10

**Latest consolidation scope:** follow the [first-adoption review](vesper-shared-design-language-consolidation-2026-09-10.md#21-first-adoption-review--entity-and-social-september-11).
Entity's adoption is accepted as described above, not an invitation to optimize this
project's selected composition. Preserve hierarchy, anatomy, meaningful imagery/
instruments and coherent density variants; unify construction and fix obvious
inconsistencies. Do not shrink, recard, remove affordances or replace a selected
layout merely to fit existing code tokens. Existing coverage and product
corrections remain; optional redesign is separate. The shared review has
happened; do not reopen the old adoption wait or Stage 1 correction assignment.

For Entity specifically, preserve **14 R2 — Opened for Saturday**, including
the media/identity opening, paired facts, attributed narrative and compact
practical/map footer. Workbench 02C's proposed Print Room fact-list/card
composition is not its approved replacement. Compare the same Hortus R2
before and after extraction. Existing source/support and authority corrections
remain open; visual preservation does not endorse unsupported fixture claims.


Use the [shared extraction and consolidation brief](vesper-shared-design-language-consolidation-2026-09-10.md)
for the bounded library/translation pass. Preserve the stable shell, sans identity and purpose-responsive reading; align original readers/actions and compare at a common viewport.
This supplements the selected product direction below; it does not adopt proposed
token values, replace this project's experience, or restart Integration. Earlier
residual lists are dated review evidence: verify the current export before
reassigning a correction that may already be delivered.

### Coverage follow-through — September 10

Use [audit E1–E4](vesper-shared-design-language-consolidation-2026-09-10.md#entity--resolving-and-opening-the-right-thing)
for the donor/gap evidence.

| Preserve / reuse | Coverage work to carry into the shared library |
| --- | --- |
| 05 candidate/opening/made/matched/failure paths | Carry the originating row and target through resolution. Distinguish temporary failure/retry from unsupported identity; a finished detail page alone does not cover entry. |
| 06B sparse shell, requested result and next open | Supply the requested Read up/check pending and failure treatments, currently partly annotation-only. Keep 14's selected purpose-responsive body; no research by opening. |
| 04 byline/overflow geometry and 14 exact original | Reuse anatomy only from historical people states. Do not reintroduce Useful/usage reports/ambiguous Keep as a standard action strip; receive current Social/Life meanings. |
| 10 finite targets and 12 external handoff/return | Reuse bounded target choice only when it changes the effect. Label ordinary external destinations clearly; no universal interstitial and no claimed confirmation on return. |

**Bounded handback:** the same place from Places candidate to resolved/sparse
receiver, one requested-check state and return, plus the shared original reader
where appropriate. Coordinate with Places on entry/return and Social/Life on
authored material. Preserve selected 14; do not reopen the invariant-body
decision, add purpose modes or create another Entity design project.

**Sequence and responsibility:** Stage 1 is composed by the shared-library owner
in the shared workbench; the six original projects stay unchanged until review.
This project supplies selected donors and identifies missing variants. Adopt
reviewed shared definitions afterward, rather than creating its own control
library. For each item, record existing donor / targeted addition / owner
dependency / proposal / later native check, with an owner and next trigger.
Design coverage is not implementation or verified behavior.

## Current design assignment — September 9 six-project consolidation

### Selected direction and residual corrections — September 9

**Decided: accept board 14's purpose-responsive direction.** Stable identity
and shell do not require an invariant body. The
[accepted decision](../decisions/2026-09-09-select-design-convergence-and-prepared-continuations.md#1-select-the-purpose-responsive-entity-destination)
resolves the choice; do not return the same comparison for another ruling.
Board 14 is delivered by Places, not missing. Its individual fixture claims still
require correction. This export contains 21 HTML boards including references.

1. **Reconcile active 09/11/12 with 14.** Discovery, Saturday assessment and a
   supplied reservation receive different useful supported readings. Remove
   weeknight advice from Saturday and Reserve from the reserved view's primary
   action. Preserve confirmation, arrangement and approach. Retire the literal
   invariant-body/two-fact ceiling where it blocks useful understanding, not the
   restrained photographic/minimal-body language. No research on opening.
   These are purpose-sensitive readings, not modes the user must classify first.
   Preserve a kept exact result or original at its permitted version and expose
   current information separately when needed. Do not reshuffle a reader in use
   because ranking changes; a relevant live correction can be signaled without
   replacing its subject, authored material or return position.
2. **14 R1–R3 source discipline.** Establish or remove the causal terrace → small
   kitchen → set-sequence claim, edge-table capacity, last full service and level
   approach. Dana's displayed words do not supply all of those facts. Do not
   invent support to preserve an attractive sentence; generic source labels are
   not the missing evidence. Preserve a substantive supported reading instead.
3. **14 R4 / 04 / 12.4 original route.** Open Dana's original first, with human
   Reply and separate Place access. Name the actual subject of Keep; optional
   private Ask requires eligible AI use. Use current Life 07.7 / Social 08 and
   the exact-original display contract, not assumed independent-copy rights.
   Remove automatic “SHE CAN SEE THIS USE” from the selected route. A private
   use is not a sender acknowledgment; no promise to erase received screenshots.
4. **14 R5 / Places 08.13:** select meaningful unvisited understanding but fix
   the diagram to show the actual pumps/gates contrast or keep concise prose.
   Do not require a route, generic dossier or invitation to visit.
5. **06/06B / 12.1–12.2:** remove catalog/queue narration from sparse consumer
   copy; retain useful identity, uncertainty and explicit Read up. Select clear
   external-destination labels over an extra interstitial for ordinary Maps or
   restaurant links; retain a confirmation only for a real consequential choice.
   Source/time labels conveying evidence must remain readable at larger text,
   not be frozen merely because they are called labels.
6. **00/02/07/08/13 selection map:** include 14, remove active Add-to-trip and
   research-on-open mirrors, and distinguish selected design from dated code
   claims. Do not recertify September 4 implementation through this review.

**Handback:** reconciled selected frames and index, actual support, exact
Life/Social/Places receiving states, and full-scroll/larger-text evidence.
No new project, generation on read, new owner, production change or Integration
restart. Historical rationale below is not a second current queue.

### Historical assignment rationale — September 9 reviews

**Dated Entity entry and reasoning.** Reuse
the lab itself and the [six-project handoff map](vesper-five-project-design-consolidation-2026-09-08.md#current-handoff-map--september-9).
The durable source is the Claude Design project `dd48304b`; `Downloads/vesper-entity-object-handoff-lab`
is its local export, present in the latest September 9 review. Earlier absence
reports were dated observations, not its current state.
An export README naming a single board to implement is packaging, not an instruction.
The September 3–4 rulings and implementation report below retain their dated
rationale; this review does not re-certify their code claims, rewrite founder
rulings or activate engineering. The latest export includes 21 HTML boards,
including reference/history, rather than only the earlier 00–07 working set.
Keep the square photographic plate or no image, restrained shell, readable short
paragraphs, minimal verbs, source attribution and exact-origin return. Do not
restore the rejected section-heavy page or turn this into a new design project.

### 1. Consistent identity, purpose-responsive reading — decision comparison

**Historical second-pass scope, September 9:** the comparison was then largely
undelivered; Places 08.7–08.9 already existed. Entity 14 has since arrived and its
direction is selected above. The [earlier shared scope](vesper-five-project-design-consolidation-2026-09-08.md#second-pass-strategy-alignment--september-9)
preserves that rationale, not an outstanding decision request.
Keep sections 1–3's concrete corrections, with the additional value tests and
handoff requirements below. This is not a fourth Place mode or another redesign.

Entity 09 permits a context line, fact-pair order and first verb to change but
forbids changing “a sentence” of the body. The observed consequence is:

- **09C:** Saturday consideration still recommends going on a weeknight.
- **12.3:** the context/lower row says reserved, while the pair says Reserve and
  the body still recommends booking.
- **09B:** four generic paragraphs precede Dana's contribution that motivated
  opening the page.
- Price/reservation facts repeat in the pair and closing row rather than each
  row earning its place.

Recommend relaxing the lab's invariant-body rule, **not the founder's visual
choice**. Product Model §4.1 and Expression/Projection Canon §7.3 allow emphasis,
density, actions and visible evidence to respond to purpose without changing
identity or ownership. Compare the same Hortus opened to discover it, assess
Saturday and use an existing reservation. The last reading should help that
evening work rather than keep selling the place. Select or omit already-supported
paragraphs/facts as appropriate; this does not authorize generation on opening,
new provider calls, inferred taste, or a new stored personalized Take.

**With Places 08/Entity 11:** decide what the shared place reading preserves.
Places' stairs/waiting/approach comparison provides consequential information the
two-fact template does not currently express. Entity provides consistent chrome
and exact return. Neither project's entire layout should automatically overwrite
the other. Compare actual entry purposes; “show what my friend sent” may open
the original directly. Return the selected treatment and any requested amendment
to the founder; do not claim a universal renderer or accepted new anatomy.

### 2. Exact human originals and coherent consequences

- Reconcile **04/12.4** with Social 08 and Life's current accessible/kept treatment.
  Keeping a place, retaining a friend's words under a grant, Reply and private
  Ask are different helpful choices, not a mandatory action inventory. Preserve
  human Reply where the person is receiving the original; useful/keep/send onward
  does not substitute for it. Neither opening nor refinding means permanent Keep.
- **04.08** still exposes automatic “USED IN TONIGHT / SHE CAN SEE THIS USE.”
  Social explicitly excludes this behavior. Do not carry it into the selected
  shared route as a settled rule; name the unresolved visibility decision.
- A Life-owned reader does not force the Life tab active. The accepted origin-
  stack navigation keeps a Home arrival in Home until a deliberate root crossing.
  Preserve 09's exact Back and Life's query/containment restoration; reconcile
  the actual source/arrival rather than recoloring tabs by object ownership.

### 3. Useful thinness without procedural explanation

- **06/06B sparse page:** three paragraphs about catalog, missing writing, queues
  and research explain Vesper's internals instead of helping someone with a café.
  Lead with useful known identity/location and the available explicit Read up
  path. Preserve material unknowns and no research on read; do not fill the page.
- **12.1/12.2 external links:** the new Directions/Reserve interstitials are
  design-only proposals, not mandatory confirmations. Compare an explicit
  external-destination label with a second screen; keep a confirmation only when
  it resolves a real ambiguity or consequential choice. An external booking link
  neither books nor starts monitoring. Do not add booking infrastructure.
- **06B large text:** “labels, not reading” is insufficient reason to freeze
  source/time labels that affect interpreting advice. Include a focused larger-
  text readability comparison; no native accessibility certification follows.

### Handback and boundaries

**Test understanding as well as going somewhere.** Discovery, visit assessment
and using a reservation are necessary comparisons, but can all remain variants
of “should/how do I go?” Product Model §3 permits causal, historical, perceptual
and relational understanding, including an unvisited Place that explains another.
Use one existing supported Print Room/Red Hook or coast comparison with Places:
does the actual distinction that motivated opening survive this destination,
or collapse into generic paragraphs and a dossier link? Preserve the restrained
photographic/minimal-body ruling. Select supported material and an appropriate
existing expression; do not generate on read, require a richer page everywhere,
create a new owner or impose a universal two-fact ceiling on useful understanding.
Places leads the situated encounter; Entity provides its recognizable destination.

**Use current Life/Social donors.** Life 07.7/07.8 and Social 08 now draw the
original reader with independent Reply/private Ask and an optional retained-copy
proposal. Do not copy the older missing-Reply set or automatic use-visibility
receipt as settled behavior. Reconcile what is accessible, deliberately kept,
and revocable; private reading is not social acknowledgment. Keep exact origin
and distinguish direct-original arrival from reading about the Place. Entity
10's softly held intention receives Life P1's selected later-use comparison;
“it appears in Life” alone does not establish warranted future elevation.

**Preserve useful thinness, not procedural homework.** Sections 2–3's sparse
page and external-handoff comparisons remain current: give useful known facts
instead of catalog/queue explanations, and compare clear external-destination
labeling with an extra confirmation screen. A second screen must resolve a real
choice rather than document our architecture. Repeated price/reservation facts
and unscaled source/time labels still need the focused comparison, not a new
component catalog. Readable attribution is part of the useful content.

**Decision and delivery record:** return the same-identity comparison with the
useful reason for each reading, the exact amendment requested to invariant-body
selection, and the founder ruling it preserves. Correct index/build-reference
drift: old research-on-open wording cannot supersede explicit research, and
September 3–4 implementation notes are dated observations rather than present
capability. No policy is adopted by drawing an alternative. This round's evidence
is source review; new rendered or wired checks must name the actual frames and
limits rather than inherit an older delivery claim.

Update the existing export index and handoff with exact changed frames, the
Place-purpose comparison, selected/proposed/reference status, sibling deltas and
remaining reading/prompting/checking work. Preserve known/matched/made and failure-
preserving origins (05), conditional research and no-photo states (06/06B), and
place/history reverse doors (11B/C); these already exist. Inspect affected full
scrolls and large text, separating wired interaction from static boards.
No new owner, source-use/grant policy, research runtime, production implementation
or publication is authorized. Integration remains paused. Accepted rulings below
remain in force until explicitly amended; comparisons here remain proposals.

## Historical implementation status — September 3–4, rev 6

**Status at that handoff:** design handoff from the Claude Design project *Vesper — Entity Object Handoff Lab* (`dd48304b`), reorganised 09-03: boards 00–07 working set (**06 = the page**), Z1–Z5 archive. Rev 6 records the explicit research-request boundary and viewer-scoped people-line projection alongside the guarded mobile object-page skeleton/ranker and read-only persisted research projection. Rev 4's photography-or-nothing ruling and Rev 2's other rulings still hold. The entity-resolution brief's Part I rules (identity, privacy, source use, no Take on open) stay locked. Non-canonical until the remaining contracts are promoted.

**Symbol audit, 2026-09-09.** The names this section cites were re-checked
against the checkouts below. Present as written: `ObjectPageRebuild`,
`objectPageProjection.ts` (ranking on `near` / `localHour` / `occasionLive`),
the three feature flags, `paragraph_sources`, `display_policy.map_surface`, and
`presentation.facts[].source_mode / observed_at`. The four routes exist, one of
them (`research-status`) previously missing from §3 and now added. Every
component under **Delete from the page** is absent from `ObjectPageRebuild.tsx`
and survives only on the legacy venue/experience routes behind the flag. The
sixteen commits cited on lab board 13 all resolve, but split across two
repositories: twelve in `travel-app`, four in `travel-agent` (`67e4f6556`,
`180991b63`, `4f018f2f0`, `1aa154018`). Board 13 was unlabelled and has been
corrected in the lab on 2026-09-09: every hash now carries an `app` or `agent`
prefix and the footnote states the convention. Checked at
workspace `913374c`, backend `a7c02cbe1`, app `e2e792913`, with the workspace
tree dirty. Re-checked 2026-09-10: the workspace has moved to `184ce4f`, but
`travel-agent` and `travel-app` are still at those exact revisions, so every code
claim above stands unchanged; only the docs repository advanced. **This is source inspection for documentation accuracy, not an
evidence layer:** no `contract`, `database`, `device_mock`, `persona_replay`,
`staging`, `physical` or `ai_eval` run was executed, and nothing here promotes a
receipt or converts a dated September 3–4 claim into present capability.

**Residual, 2026-09-10 — board 14 and the address.** Board 14 draws
`Via Rota 20 · Sorrento` in the where row of R1, R2 and R3, and its terms say the
preserved shell includes “the map with the seeded identity and the address”. No
address field exists on `EntityDetailPresentation`; the where row ships locality
plus `display_policy.map_surface`. The board labels the value a fixture and says
so in its status, so this is a shell claim to correct rather than a drawn lie,
and it does not touch the purpose-responsive body direction that was accepted.
Founder call: either the where row stays locality until an owned catalog field
lands, per board 13's ADDRESS delta, or that field is prioritised. The Places
lane supplied board 14, so this lab should not edit it unilaterally.

## 0. Rulings of 2026-09-03 (founder, in chat)

1. **Seed identity, not opinion.** Seed lightweight canonical entities broadly — name, kind, coordinates, address, provider ids — from open datasets (Foursquare OS Places, Overture, OpenStreetMap; never Google). "No backfill" in the brief was a fence around one engineering slice, not product law. The named cost is dedupe: two sources for one place must become one `EntityRef` before pins can land on it.
2. **Web search on demand, never on the read path.** *Corrected 2026-09-04 to the shipped and roadmap-approved trigger:* research runs only on an explicit **Read up** request (`POST …/research-requests`, idempotent, flag-gated); a base page read never enqueues, calls a provider, or spends. The result lands as a persisted, sourced brief with an as-of, cached for everyone, refreshed only on explicit request. It describes; it does not judge. The provider-only row becomes the long tail; the brief's three destinations collapse to **known / matched / made**.
3. **The interpretation is a composition.** One paragraph block in Vesper's voice built from: the cached web brief (LLM); Vesper's dossier or angle when one touches the place (governed, optional input; the only source of a verdict); and *today* (deterministic, per viewer, no model): open-until with source and as-of, your keep and line, friends' lines by grant, whether it is in tonight's Occasion. **Cite, never absorb:** friends' words appear verbatim, attributed; today-facts and quotes are injected by code, not generated. No inferred taste, ever. This retires the stored-Take-per-(entity, mode) model and its mode-key defect.
4. **The plate, square, and one blob.** Adopt the plate full-bleed at the top with **square corners**. **Imagery ruling (later 09-03): a photograph or nothing — no riso, no illustration, no stock.** A friend's photo appears only behind her mark. No sections: the page is the plate, the name, **one readable body of generated text split into short paragraphs**, a strip of faces for who has been, the verbs as text, the metadata rows, stop. Sections were "too messy"; the restyled-sections board was rejected, and the blob board was accepted (both retained in the Z archive).
5. **No "Add to trip".** The trip picker / day picker / review sheet is the travel-app grammar and does not belong on the object page. Verbs: **Keep** (top bar; a kept place is the pin) · **Ask Vesper** (Chat) · **Leave for someone** (the directed act; the dark addressed-handoff) · **Tonight?** only while an Occasion is live on Home. No primary button. The in-plan state becomes a sentence in the body ("it's in tonight"), not a strip.
6. **Citations inline, mixed.** The body cites the way a good answer does: small inline markers after the sentence they support, mixing web sources and people — `[1] [2]` for the web, a face for a friend, an umber face for you — with a sources line under the body listing them. A marker opens its source (the page, or the friend's line verbatim with its grant).

**Execution:** the agent-ready brief is `docs/working/object-page-rebuild/build-brief-2026-09-03.md` with `fixtures.json` beside it (token map, components, contracts, ranker table, composition template, state machines, acceptance tests, PR steps). This document remains the *why*.

## 1. What is being replaced

Three implementations of one object: the bespoke venue route (18 render units, 4 action ladders), `EntityObjectPage` (site only; no verbs, no hours), `ExperienceDetail` (rail last, no status, no not-found). Accommodation stays outside by decision. The rebuild is one page component for venue · site · experience · neighbourhood-as-entity · spot, with the container page sharing chrome and carrying the map as its hero.

## 2. The page (board 06) — the spec

```
plate        a photograph or nothing: your kept photo → a permitted provider photo fetched live with its credit → NO plate (title under a warm bar). Never an illustration, stock, or a gallery. Containers and spots use Vesper's map as the plate
top bar      back · Keep (bookmark) · share  (share absent on an owner-private shell)
kicker       CUISINE/KIND · TOWN   (town is the door to the container page)
name         System Sans 600 24/27
byline       faces of who has been (people ink, you umber) + one label, e.g. "Dana and you have been here"; absent when no one
the pair     the two most relevant facts, two columns over one hairline: value 17–18px, mono label, mono source line with as-of
body         2–4 line paragraphs at 16.5/24 serif, one thing each; inline citations: numbers = web sources, faces = people; friends' words verbatim in italics; the last paragraph is the only uncited one
sources      the numbered list + the people cited (may fold to one line)
verbs        text: [Tonight? · only while an Occasion is live] Ask Vesper · Leave for someone
the rest     one two-column row of the demoted facts (PRICE · TABLE with a Reserve link when real; HOURS; ACCESS when known)
where        locality · map snippet (96px thumb beside it on the entity page; full-width strip on the container page) · Directions → provider surface   [NO street address: the wire carries locality + `display_policy.map_surface` only; an address line needs an owned catalog field first]
end          paper
```

**Dynamic facts.** Facts are one ranked list, not fixed slots. The pair takes the two most relevant to the situation; the closing row takes the rest; nulls are demoted as honest unknowns ("not checked" + the live check; "unknown") or dropped; a null never occupies the pair. Ranking is a table on three signals the page already has — near the place (explicit one-shot location), open now / evening, Occasion live — with no model in the loop:

| Fact | Arriving (here, evening) | Planning (elsewhere) | Null |
|---|---|---|---|
| Open until | pair | below, as the week's hours | below: not checked + live check |
| From you | pair | below, from the town's anchor | absent |
| Price | below | pair | below: unknown |
| Table | below (+link) | pair (+link) | absent |
| Where | closing row | closing row | pair when little else is known |
| Access | below when known | below when known | absent |

**Rules that survive from the law (board 02):** each input has one owner; absent stays absent; nothing generated on the read path; the sparse page ends above the fold; removing one input removes exactly its sentences and its marker. Today-facts and quotes are injected verbatim by code, never by the model; markers are attached by the code that placed the sentence.

**Rejected on the way here (kept as record):** the restyled-sections and refinement boards in the Z archive, the map strip on the entity page, "Add to trip".

## 3. Inputs → wire

| Input | Reads | Change needed |
|---|---|---|
| Identity | `entity.name`, `ref`, `lineage`, `categories` / `venue_type` | render city when neighbourhood absent; "also listed as …" provenance line for a matched candidate |
| Web brief | **landed read + request slices**: `EntityResearchBrief {text_paragraphs, sources[{number,title,url,retrieved_at}], generated_at, expires_at}` plus `EntityResearchRequest` | `GET /api/entities/{type}/{id}/research` exposes completed persisted briefs only; `POST /api/me/entities/{type}/{id}/research-requests` is an internal, idempotent explicit queue tap. It is disabled by default, never runs on GET, and does not support owner-provisional or experience shells. `GET /api/me/entities/{type}/{id}/research-status` returns that viewer's request lifecycle (`EntityResearchStatusResponse {status, request_id, updated_at, brief_generated_at, brief_expires_at, retryable}`) so queued / failed / stale states render without a read-path call. |
| Dossier / angle | existing governed pipeline, `place_slug` link | optional input to composition; the only permitted source of a verdict |
| Today | `status {operating, open_now, hours, as_of, sources}`, `relationship {saved, encounters, active_trip}`, your line, **people_lines[]**, tonight's Occasion | deterministic template, per viewer; unify `status.hours` / `tail.hours` |
| People | **landed bounded projection**: `EntityPeopleLinesResponse {entity_ref, lines<=3 {author {id, display, monogram}, made_at, text}}` resolved viewer-relative server-side from accepted exact-place UUID handoffs | byline renders only when the relationship owner is enabled; quote/body citations, media, and mark-opened sheet remain future work |
| Metadata rows | `status` + venue tail `cuisine_type, price_range, price_per_person_estimate, avg_duration_minutes, reservation_required` + `presentation.facts[]` with `source_mode / observed_at` | render with provenance; stop dropping it at the mapper |
| Verbs | Occasion-live flag from Home; Chat seed; addressed-handoff create | remove itinerary ladders from the page |

## 4. Components

**Keep:** `ObjectPageShell` chrome (plate geometry now square), `SpotTopBar` (Save/Keep = reference verb; share constrained on owner-private), `ObjectPageStateShell`, `StayLocationMap` with `mapSurface` (for the instrument top), the title register (`objectTitle` sans 600 24/27), `PlaceShareOwnerSheet`.

**New:** `ObjectBody` (paragraphs + inline citation markers + sources) · `PresenceByline` · `FactPair` + `FactRanker` (the table above) · `ClosingRow` · `WhereRow` (locality + snippet + directions handoff) · `InstrumentTop` (sparse hero fallback) · `CandidateResolvingRow` · `OriginStopCard` (retryable / honest) · `SpotPage` admission · `ProvenanceLine` · the research job + cache · plate source resolver (your photo → permitted provider photo with credit → none). The guarded mobile skeleton, deterministic ranker, explicit research request boundary, and bounded people byline are now landed; richer citation, handoff UI, and provider-photo pieces remain gated.

**Delete from the page:** `SpotPlanningRail` and the trip/day/review ladder, `ItineraryStopStrip`, `WhyForYouCallout` and its route param, `OrderSkip`, the `Details` drawer, `WorldSection`, `AskVesperBlock` disc, `SpotTake` (streaming personal Take), the one-photo scroller, `CatalogEvidenceDisclosure` from the ordinary path, `EntityInterpretationBlock` (unwired; superseded by the composition).

## 5. Sequence

0. One page component; square plate; metadata rows with NOW first and provenance; text verbs. Zero new data. (Fixes buried hours, unrendered price/cuisine, three block orders.)
1. Lightweight identity seed for one city + dedupe rule; candidate paths collapse to known / matched / made. (No broad backfill is authorized in this pass.)
2. Explicit `Read up` request → existing `research_queue` item with idempotency; `research_brief` remains cached and sparse until the worker completes. `ObjectBody` currently renders persisted paragraphs and explicit source markers only.
3. Dossier as optional input; verdict only from it.
4. `people_lines` API behind the UUID relationship owner; faces in the strip for exact-place addressed lines. Friends cited inline and the mark-opened sheet remain gated until their grant and UI contracts land.
5. Spot admission (person-made places) and owner-shell promotion.
6. Live-details check; share contract for owner-private; `map_surface` on the wire.

## 6. Open questions for engineering

- Dedupe/conflation across seed sources (Overture ids as the spine?).
- Research job cost/TTL; what "sources" may be shown for a given provider's terms.
- Temporary vs non-retryable taxonomy on `POST /api/me/entity-resolutions`.
- Owner-private shell promotion when a second person reaches the same external ref or a grant touches it.
- Whether a kept place and a pin are one record or two.
- The Occasion-live signal the page reads for "Tonight?".

## 7. Fixtures and boards

Hortus (real dinner; body, hours, prices fixture) · Café Aurora (brief fixture B) · Aurora Bar / Bar Aurora (C) · Museu do Azulejo (A) · Harbor Textile Archive (E) · Sorrento (container) · "The stairs at Marina Grande" (spot). Boards as of 2026-09-03 (08–14 were added later; the lab now holds 21 including Z): 01 audit · 02 law · 03 three kinds (on the page) · 04 people slot · 05 five paths (on the page) · **06 the page** · 07 this handoff · Z1–Z5 archive (languages, blob, typeset, element order, the pair). Deleted: the restyled-sections and refinements boards. Raw code inventory: `social-aperture-generators/object-page-audit-raw.md`.
