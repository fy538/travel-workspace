# Claude Code — Travel Workspace

@AGENTS.md

Shared engineering guidance is imported above. Claude Code path rules and
commands in `.claude/` route to the same owner docs. Use `/memory` to inspect
loaded guidance; a file existing on disk is not proof it was loaded.
